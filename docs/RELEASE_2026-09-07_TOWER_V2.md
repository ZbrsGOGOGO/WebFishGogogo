# 2026-09-07 合成塔防与反馈归档发布

## 发布状态

已完成代码推送、生产部署、线上验收及两条已修复农场反馈的状态归档。

- 用户已确认合成塔防首版开发，并要求将已修复的反馈改为已完成；沿用已授权的验证后自动推送、备份与部署流程。
- 本地与 GitHub 开发基线为 `acb0660ae7f0ae908db0e1e941ba017664496d1b`，分支 `feat/workstation-tower-defense`。
- 开始本轮时生产 API/Web 均为 `d8e814e0e681af43a8e584846c6bdba7d12fbce6`，健康检查正常。它同时是本轮应用回滚目标。
- 功能提交 `ba210647a5b550e62a7d4c5da9619a77bc8cc105` 已推送至上述 GitHub 分支，生产 API/Web 均运行该 SHA 标记的镜像且健康。发布后补充本记录的文档提交不另行重建镜像。

## 交付范围

- 工位塔防改为绿植产币、商店零件、三合一与部署的局内循环；五条办公用品塔线、唯一工位守卫、重新配置的三波短局，以及原生 SVG / CSS 战斗与合成表现。范围和边界见 [V2 约定](./WORKSTATION_TOWER_DEFENSE_V2.md)。
- 仅本机保存 V2 最高分，保留旧 V1 与乐斗存储；不接入社区资产、农场经验、正式排行榜、付费、离线收益或多人 Boss。
- 添加服务器专用 `development:complete` 归档工具：精确提案 ID、版本、完整发布 SHA、确认串和原因缺一不可。事务内更新状态、追加系统审计与站内通知，重复操作不重复写入；网页权限保持不变。
- 状态时间线由白名单系统审计投影，不创建虚假用户，不把任何核心成员提升为 admin，不冒充提案作者。两条农场修复已核验并归档；塔防原提案包含后续阶段，仍保留待处理状态，不将首阶段上线误记为整条完成。

## 验证与数据保护

- 最终全量后端 97 个套件、509 项，以及前端 107 个文件、562 项全部通过；全仓类型检查和社区模式生产构建通过。
- 引擎 19 项包含 100 个种子各 240 步操作的不变量检查；真实购买与刷新、合成三阶的固定种子模拟约 112.56 秒通关，分别覆盖不移动/不放技能和主动脉冲两档。仅保底二阶塔不经营会在第三波失守。
- Firefox 桌面与 390 像素手机视口实际完成绿植购买、零件合成、部署；手机无横向溢出。按截图提升中文字号与对比度，补充商店金币余额、按钮焦点与底部导航避让。
- Firefox 实际浏览器对局购入九件零件、花费局内金币刷新商店五次并合成三阶，不移动角色、不放技能，112.892 秒成功，核心剩 3，得分 4275；期间未捕获运行错误。暂停冻结金币、主按钮回焦点到棋盘通过；只请求本地模拟的登录与协作权限接口，没有请求塔防结算或社区资产 API。这是本地真实浏览器验收，不冒充生产账号端到端测试。
- 运维时间线保留兼容旧页面的系统展示对象：明确 `kind=system`、公开号为空，数据库审计 actor 仍为空；旧页面只读取显示名也不会崩溃。
- 本轮不增加迁移，不修改 Compose、数据卷、现有功能开关与账户权限。数据库版本保持 `1700000000026`。
- 发布前建立本次独立的加密数据库备份、异机副本和完整恢复验证。用候选镜像连接无生产网络的临时 PostgreSQL 克隆，验证状态归档的事务回滚、并发幂等、作者通知与私有时间线后再操作真实提案。
- 仅更新 API/Web；保留 PostgreSQL、Redis、Gateway 的容器身份及数据。应用回滚只恢复 `d8e814e` 镜像与发布前配置，不回退数据库，不撤销真实反馈或通知。

## 生产验收与反馈结果

- 公网检查脚本完成 55 项检查；首次执行遇到一次 TCP 443 连接超时，未修改脚本重跑后全部通过。另行验证实际 HTTPS 登录、Host-only / Secure / HttpOnly / SameSite Cookie、Origin 拒绝、刷新令牌轮换、退出和普通账户不能访问开发反馈。
- 实际首页引用新入口 `/assets/index-Cgd3EdtM.js`，塔防分包为 `workstation-tower-defense-DRL6TZYd.js`；五塔、绿植、商店、背包和 V2 存储标识均出现在已部署资源中，返回 JavaScript 而非 SPA 回退页。就绪接口数据库检查正常。
- 新建受限的临时普通测试账号，通过真实 HTTPS 验证首次免费、旧玩家余额不足的准确提示、切换可负担作物后恢复种植、重复请求只扣款一次、收获后不足续种仍保留奖励及重复收获不增发。测试结束删除该精确测试身份及其关联测试数据；再次只读核验无测试账号残留，未改动真实玩家的农场或资产。
- 两条已上线农场反馈分别从版本 1、3 更新为 `done` 的版本 2、4；各自新增一条系统审计和一条作者站内通知，修复依据为原农场发布 `d8e814e`，并记录本次复验说明。原附件保留。塔防完整提案仍为原版本 2，没有整条关闭。
- 归档后核验现有账户角色、开发成员授权和迁移版本均未改变；没有为归档创建管理员账号或扩大网页权限。

## 备份、验收证据与回滚

- 本次服务器备份目录：`/opt/webfish-backups/tower-v2-20260907.wQiUF0/`。加密数据库备份、校验文件、发布前环境与应用版本均已留存；异机副本保存在操作者机器受限的 `webfish-backups/tower-v2-20260907.wQiUF0/` 状态目录，密钥和环境文件不进入 Git。
- 当前备份已完整恢复至隔离的 PostgreSQL 16 临时实例，验证迁移 `0026`；候选 API 镜像在无生产网络的恢复库中通过版本冲突拒绝、通知失败整笔回滚、八路并发只归档一次、重复操作幂等、私有系统时间线、作者通知及账户角色不变检查。临时数据库容器已移除。
- 全量测试、生产构建、公网检查、线上资源、恢复验证、归档及归档后状态核验日志保存在本次受限发布目录；私有反馈明细不进入公共仓库。
- 回滚脚本位于上述目录的 `rollback.sh`，以完整新版本 SHA 为确认参数，只恢复发布前 API/Web 和配置；已准备但未执行。PostgreSQL、Redis、Gateway 没有重建，数据库和已完成反馈不随应用回滚撤销。
