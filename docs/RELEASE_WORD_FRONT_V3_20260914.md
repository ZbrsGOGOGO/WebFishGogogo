# 文字战线 v3、免费双人房间与妖塔技能发布记录（2026-09-14）

## 已上线范围与边界

- GitHub `main` 的应用提交及生产运行提交均为 `d641ae0b0d6341a6ac3ed69db58e6ddd521362c1`。北京时间 17:46 起按 API 就绪后 Web 的顺序切换，17:48 完成公网验收。后续文档提交不作为线上应用版本。
- 文字战线 v3 单机六章、30 波无尽及独立 `word_story_v3` / `word_endless_v3` 榜：后段固定岔路、催办/故障短时干扰、白绿蓝紫金五档仅局内装备，继续使用 10+2 招募与 40 点局内经费上限。原 v1/v2 入口、服务端回放语义、旧榜及原工位塔防保持。
- 新免费 1V1 真人临时房：可选房主密码，不使用邀请码；同章节同种子的红蓝双线、服务端每 850 毫秒推进、击败来客送有限援军。房间采用 **v2 对战规则而非 v3 单机规则**；浏览器重连可回到同一 API 实例内的房间，API 重启或回滚会结束未完成房间。房间不计正式榜、办公币、成就或账号装备。
- 九层妖塔新增免费主动技能 s19「嗜血」与 s20「镇魂喝」，并补齐装备/升级预览及兼容回滚桥。未新增真实付费、权限、赠送或办公币规则。
- 两项大型反馈仍为进行中：塔防分项 4/9 完成、妖塔 v0.11 分项 6/11 完成；局外商店、魂珠、其他技能、真人平衡、正式替换及跨 API 重启恢复未宣称完成。

## 回归、隔离演练与数据

- 最终本机后端 **161 套 / 1602 项通过**、5 套 / 57 项按既有条件跳过；前端 **197 文件 / 1687 项通过**。全仓类型检查与完整构建、社区前端构建、部署预检及 `git diff --check` 通过。房间开局另以六章、五类保底英雄共 42 局固定种子回归。
- 两份新鲜生产数据库与完整环境加密备份均校验并复制到另一台机器；切换前档案的 SHA-256 为 `64ca959694f8e5d9f089124bbf7bfe03e77d8958f092408cb43dfd4904aacbf3`。隔离 PostgreSQL 16.14 完整恢复：起点 39 条迁移、148 张业务表、245 条 Arcade 对局与 8 条最佳成绩，与当时生产快照一致。候选镜像在隔离库只执行 `1700000000039` 一条 UP，终点 40 条迁移、148 张业务表、0 未验证约束、无待执行迁移；两张 Arcade 表原行数及内容哈希不变。
- 最终候选镜像对真实隔离库完成 v1/v2/v3 服务端回放与成绩结算；新兼容桥拒绝 v2/v3 新局成绩且不写最佳榜，仍可结算无版本 v1 局，并能读取、训练及保留 s17–s20 的新妖塔存档。房间服务演练验证密码、成员权限、幂等、同拍推进、退出判负和 Arcade/钱包四表内容不变。
- 最终候选 API/Web 在仅内部网络的隔离环境运行，两个随机合成账号经 Web 代理完成注册、认证、密码房创建/加入、错误密码 403、非房主开局 403、房主开局、合法招募、同动作幂等/冲突及双方 tick 轮询。隔离 HTTP 期间正式成绩和钱包表无新增/更新。测试容器、内部网络与克隆数据卷已按确切名称清理；加密备份保留，可再恢复。

## 生产发布与验收

- 生产只执行 `0039` 一次，终点 **schema39 / 40 条迁移 / 148 张表**，0 未验证约束。原 245 条 Arcade 对局及 8 条最佳成绩保留。只切换 API/Web；PostgreSQL、Redis、Gateway 的原容器 ID 与数据卷未改变。新 API 镜像 `sha256:e38805ec45b691816b270e7cdb4b3359d9dc5cd4802b56b94b5cb16d02bfe20f`，Web 镜像 `sha256:81abd64f421235ae521f55b5b7d2e5c40213e90e8e80af36e40829a2fbfd19d0`。
- 生产功能开关 `FEATURE_WORD_FRONT_ROOMS_ENABLED=true` 已由同一候选环境同时用于 API 与前端构建，其余旧环境内容未改。健康/就绪均通过，公网社区烟测 **65 项 HTTP 检查通过**；v1/v2/v3 路由、两个 v3 榜及原入口均 200，未登录房间 401、未知子路由 404。没有在真实账号上做生产房间对局或浏览器多宽度视觉验收，不能把隔离双账号验收说成真实玩家平衡验证。
- 两条进行中反馈分别回填版本 5 的分项进度、审计与作者通知，未冒充整案完成。发布后共 31 条反馈：27 完成、2 进行中、另有 2 条新提交待审阅（异步猜画题库/次数与显示问题），不计入本次实现。

## 直接应用回滚

- 唯一已验收直接应用回滚桥为 GitHub `codex/wordfront-rollback-bridge` 的 `22a4b363e4052cbc8c0fb816c99e3fe13818c603`；已保留 API 镜像 `sha256:6ac3cf1f844d53e5cb199a35c218e3e4a8de211c006bc6591ccf51fad67f240a`、Web 镜像 `sha256:683a4b5c84e47b6737f1fab4c7a2ce790ddf8afcbff15c09239bfcdc08ab90f7`、切换前完整环境及仅更换 IMAGE_TAG 的桥配置。它已在 schema39 隔离克隆上实跑，而非只做类型检查。
- 回滚时先处理在途操作和临时房间通知，再只切回桥的 API/Web，保留 schema39、所有玩家数据、v1/v2/v3 成绩、妖塔存档、反馈与审计；**不运行 DOWN、不回灌旧备份、不删数据卷**。桥不能完成尚未结束的 v2/v3 对局，须在恢复新应用后继续；回切会终止内存房间。旧桥 `1f50c07` 不识别 s19/s20、裸 `9013da7` 也不具备必要的新版成绩兼容性，均不得作为本次直接回滚目标。
