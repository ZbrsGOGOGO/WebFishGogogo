# 2026-09-08 农场收益与余额发布

## 发布结果

- 应用提交：`cde4e54e0e6781cf82eb456290e296ed6af065a2`，已推送 `feat/workstation-tower-defense`。
- 生产 API/Web 已运行同一提交，健康检查通过；GitHub 未合并 `main`、未强推。
- 回滚应用：`cb471d08d021c3219bda643549b33227379124ec`。应用回退不回滚数据库、已赚办公币或收获记录。
- 收益细则见 [农场经济说明](FARM_ECONOMY_2026-09-08.md)。无迁移、权限调整或真实用户余额补写。

## 验证

- 后端 97 suites / 511 tests，前端 108 files / 616 tests，共 1,127 项通过；类型检查与完整构建通过。
- 隔离 PostgreSQL 16：八个同键并发收获仅一笔基础收益与续种；六个不同键仅一个成功，其余明确 `PLANT_NOT_READY`；旧版回执原样重放不补发基础收益；第 4/5 次仍有基础收益；通知写入失败同时回滚基础奖励、订单奖励、经验、种子支出与回执；六种作物的报价和实际收入一致；工具、技能、等级和整批舍入一致。
- 隔离恢复副本中的原有用户、钱包和农场行摘要前后相同；临时测试记录与临时容器均已清理。
- 真实 HTTPS 专用普通测试账号：登录/同源刷新/Host-only 安全 Cookie、无 Origin 刷新拒绝、开发协作权限拒绝、免费首轮、四次收获及重复请求、工具扣款与余额同步均通过。
- 真实 HTTP 低余额草莓用例：三次订单已用完、旧周期余额 15，基础到账 567，升级四块地续种扣 240，余额 342；工具升级扣 200 后查询余额 142。仅调整专用合成账号的测试状态，完成后精确清理；真实用户未被借用。
- Firefox 真实 390px 视口（文档宽度 378px）与桌面 UI 通过；余额入口可见，收获/升级同步，网络错误保留上次余额并标待同步，完整服务端收支摘要保留。UI 测试使用本地合成 API，与线上经济用例分开记录。
- 原公共烟测在服务器经 HTTPS 域名执行 55 项通过；本机固定已核实生产 IP、保留域名 TLS 校验后原脚本同样 55 项通过。此前本机代理 DNS 路径两次连接超时，失败输出保留，未弱化检查或修改系统代理配置。
- 线上入口 `/assets/index-Df79qvY1.js` 已核验含新农场收益和全局余额；原塔防 chunk `workstation-tower-defense-DviVZY_O.js` 不变。

## 备份与部署边界

- 服务器限权目录：`/opt/webfish-backups/farm-balance-20260908.xiXGJh/`。
- 异机副本：`/home/zbrs/.local/state/webfish-backups/farm-balance-20260908.xiXGJh/`。
- 发布前生成 AES-256-CBC / PBKDF2 加密 PostgreSQL 备份，清单覆盖密文、解密密钥、环境与发布身份，异机逐项校验通过。环境/密钥/备份均不入 Git。
- 先完成 network-none 临时 PostgreSQL 16 全量恢复，再用候选应用镜像共享该隔离网络执行经济验收；不提供生产凭据、不发布端口。
- 仅用 `--no-deps` 替换 API/Web。PostgreSQL、Redis、Gateway 容器身份保持不变，迁移仍为 `1700000000026`，不删除或重建数据卷。

## 反馈处理

本次“农场收入和成本”反馈已在生产验收后更新为 `done`，版本 1 → 2，并记录站长授权的运维审计和作者通知。三个待评审的塔防建议未混入本次发布。新增新闻/热榜需求另行开发，不把仅有官方外链的入口说成已完成站内实时榜单聚合。

最终只读核验：10 个真实账号均保持普通用户角色，4 个开发协作成员不变，专用合成测试账号残留为 0。
