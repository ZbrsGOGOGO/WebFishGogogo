# 协作补齐、纸上红蓝房间与两款本地游戏：正式发布

北京时间 **2026-09-09 19:40**，应用 `ff6bab4ae5aa68a14f0de0204dbd34b9eb822a82` 已部署到 `https://zbrshyyzxx.top`。随后真实公网 HTTPS/WSS、匿名桌面/手机浏览器验收与开发反馈回填通过。本记录不是只完成代码或候选测试的声明。

## 版本与交付

- 工作分支保持 `feat/workstation-tower-defense`，没有合并/推送 `main`，没有强推。
- 主功能提交 `6e388d90cd7d2030851adc1a5d2d1ae9497eaaf4`；最终视觉、房名校验与测试记录提交 `ff6bab4ae5aa68a14f0de0204dbd34b9eb822a82`，均已推送 GitHub。
- 原线上应用及回滚镜像基线 `a456a8b27027a45a7ebd10c9a4f9efd2f8ca6696`；本轮后续文档提交不改变已运行应用的镜像标签。
- 实际 API 镜像 `sha256:8f380df9a6070c1ec729b13be91d0705aa6cdc543d3cd047e7d0a7816c2e9051`。
- 实际 Web 镜像 `sha256:462d1da02dd3e4b61f20373a4cd038aac05b9bb624158851a80337b17962b345`。
- 仅启用五个新增功能开关：正式工位塔防、公司工作台、妖塔免费扩展、Paper 联机、扩展热榜。前四项前后端开关同源；既有权限、密钥、支付配置保持不变。

## 实际入口与边界

| 模块 | 入口与交付 |
| --- | --- |
| 纸上红蓝房间 | `/games/ballpoint-breach/arena`；4–8 席、红蓝队、AI 补位、可选密码、20–100 击败目标、安全随机位置复活、服务端状态与真实 WebSocket |
| 低调小窗 | 纸笔风、默认静音、最小化及工作便签遮盖；联机遮盖清零操作但不暂停服务器对局 |
| 两款新增单机 | `/games/office-2048` 数值整理、`/games/underrun` 机房巡检；同源打包、无 iframe/CDN/跟踪、静音、失焦/Esc 停止与卸载释放 |
| 正式工位塔防 | `/tower-defense`；服务器存档/时钟/结算、六章/60 波上限无尽/极限、职业/天赋/羁绊/八档升职、战报、正式榜与受限奖励 |
| 塔防表现 | 金币实际飞向绿植、真数值滚动、蓝金合成光柱、三星 200ms 震动、三种 SVG 守卫动作、刷新/种植反馈 |
| 公司工作台 | `/office`；复用既有公会，保留旧入口兼容；部门周常/公告/榜、免费收藏与皮肤、故事分支/评分、异步猜画/卧底墙、小老板日常 |
| 九层妖塔 | 原入口内增加免费来源池/兑换/成长、五神器终极、秘境/周常、自愿论道、2–4 人真实回合小队、称号及伪装皮肤 |
| 全局成长 | 35 个成就目录，服务器按真实业务投影；统一个人主页与聊天称号，不重新发放 VIP |

两款单机与 Paper 临时联机房**本期不接办公币奖励或正式排行榜**，不接受浏览器自报分数换资产。Paper 单局最长 15 分钟，空房清理；服务器重启可能结束临时房间。正式塔防旧练习仍保留 `/tower-defense/practice`，其本地成绩不导入正式奖励。

正式塔防有效败局按基础收益 30% 结算，空局零收益，单局至多 40 币、每日对局合计至多 80 币；每日三模式各一名昨日冠军 12 币，事务与唯一键防重发。公司仍沿用既有 Lv15/20000 币创建条件，不借发布提升真实账号权限或修改余额。

真实付费、概率降星商店、扣取其他玩家工资/资源均明确不实施；不将免费替代路线写成支付实现。玩法细则见 [工位与 Paper](WORKSTATION_CAMPAIGN_AND_PAPER_ARENA.md)、[公司规则](OFFICE_HUB_RULES_20260909.md)、[源码与许可](LOWKEY_GAME_SOURCE_REVIEW_20260909.md)。只引入许可已确认的 2048/Underrun，未引入授权不明的候选。

## 热榜实际验收

公网普通账号读取当日快照，以下六种来源**各 10 条、日期 2026-09-09、状态 fresh**：Hacker News、Stack Overflow、GitHub 新星、百度热搜、B 站全站榜、豆瓣新片榜。

微博、知乎、抖音仍为 `external_only`，没有伪造站内标题。B 站此前匿名探测也出现过业务风控码，现时成功不保证每次可用；失败按上游退避，保留真实旧快照并展示状态，不换 Cookie、身份或代理绕过限制。豆瓣只采新片榜标题/顺序/评分/链接，不混入口碑榜、票房榜或 TOP250，不复制正文/剧照。自动快照时间沿用每日北京时间 08:10。

## 分层测试证据

各层计数互有覆盖，**不能直接相加当作独立测试总量**。

- 后端全回归 141 套 / 1294 项通过；最终前端 162 套 / 1151 项通过；最后后端差分 102 项、塔防表现 130 项前端专项另行复验。全仓类型检查、community 构建及最终生产镜像构建通过。
- 全跑明确跳过的 23 项新 PostgreSQL 专项随后在真实隔离 PG16 执行：公司 15、妖塔 3、塔防 5，全部通过，不把 pg-mem 当真实数据库并发。
- 原账号/开发协作 9、资产 10、社交 18，共 37 组真实 PostgreSQL 大场景通过，包括行锁竞争、8 路幂等、两实例 Redis/WS、跨账号隐私、撤销及清理。各隔离业务库 146 表指纹恢复基线。
- 最终不可变镜像候选 24 项去重检查通过：两独立 Firefox、真实 Nginx/Nest/PG、原生移动/射击、密码/换队/AI、受伤死亡复活、小窗遮盖/最小化、自然达到目标后的双端一致结算；实战 **102.3 秒，蓝 20 / 红 16**。另验正式塔防/榜/公司、两款本地游戏及三个许可证资源。没有房间、分数或 API 模拟。
- 候选组合流程第一次在 Underrun 正常收起状态未满足等待条件，原失败记录保留；没有足够事件日志确认当次精确原因。未改应用或关闭保护，单页重验通过真实 WebGL/WASD、零 AudioContext、Esc 后实际 draw 调用停止，随后公网浏览器双宽度再次通过。
- 公网两个随机普通测试账号：37 次真实 HTTPS 请求，密码错误拒绝、正确入房、双 WSS 首帧票据认证及实时快照，注销后旧 JWT 拒绝；只读公司/塔防/妖塔/成长零奖励。测试账户精确清理 2 个、私有限速桶 8 个，资产变化 0、外键残留 0；不删共享 IP 桶，不启动邮件/赠币/授予 VIP。
- 公网匿名 Firefox 154.0.1：1100px/390px 共 18 页面检查通过，TLS 1.3/证书链验证正常；受保护模块真实进入登录门禁，公开 2048/Underrun 实际操作。无未捕获异常、外部资源请求或横向溢出。门禁验收不冒充私有功能验收。

私有完整日志、截图、合成测试清单及失败重验记录保留在受限运维目录，未提交用户原文附件、凭据或数据库备份。

## 备份、迁移与发布安全

1. 最终提交绑定的新鲜生产自定义格式备份，AES-256/PBKDF2 加密；完整恢复到一次性 `network=none` PG，验证旧 schema32、33 条历史迁移、136 张表。
2. 八项备份清单逐项校验权限、属主、非链接和 SHA-256，并复制到异机私有目录再次严格校验。旧版本镜像与原环境可恢复文件保留。
3. 用最终 API 镜像在另一完全隔离的生产备份副本执行 `0033/0034/0035 UP → 仅空新表 DOWN → UP → noop`；原 136 表数据/结构/序列及 VIP 行不变，新增 10 表，最终 36 条迁移/146 表。**生产不执行 DOWN 或恢复旧库**。
4. 生产只应用三条待迁移并更新 API/Web，Web 强制重建以刷新 API DNS。PostgreSQL、Redis、网关容器的完整 ID 未改变，未重建基础设施或删除生产数据卷。
5. API 于 19:40:10、Web 于 19:40:16 启动，三类健康检查及实际 tag/digest 验证正常；公网验收后才回填协作状态。
6. 验收完成后按完整 ID/标签/网络/挂载逐个核验，删除本任务 10 个测试容器及独立测试网络；仅合成 tmpfs 测试库被丢弃。先归档测试日志/元数据，保留全部主机证据、应用镜像和生产备份；清理后生产仍健康。

### 回滚

保留应用 `a456a8b27027a45a7ebd10c9a4f9efd2f8ca6696` 的原始镜像。私有发布目录 `/opt/webfish-backups/completion-release-20260909.DlYjfW` 保存 `rollback.sh`、原环境、严格备份清单及版本门禁；脚本已审查，**本轮没有实际执行生产回滚**。

紧急回退必须先验证环境未被他人更改并停止新 API 接收/排空在途写入，再回退应用；保留 schema35 和所有新存档、奖励。旧引擎不理解新版妖塔成长/新增个人数据，回退时关闭妖塔整体、自动探索和账号注销三个开关，随后以前向修复恢复。不能恢复旧数据库覆盖玩家新进度，也不能运行旧发布中的授权/VIP/删表步骤。

## 协作反馈回填

上线后重新读取全部 15 条反馈，作者仍活跃，原版本无并发变化；原始提案、附件和历史审计保留。通过现有带版本号的运维 CAS 接口写入，不直接修改状态列，不提升任何人的权限。

- 两条塔防/公司长案：分别 `v3 → v5`、`v4 → v6`，免费范围逐项完成后归档。
- 妖塔 v0.7 / v0.6：分别 `v5 → v7`、`v2 → v4`，免费玩法与成长完成后归档。
- 原已完成塔防难度：`v4 → v5`，补齐 5/5 分项，保留完成状态。
- 热榜：`v3 → v4`，新增 B 站/豆瓣已验收项，4/6 项完成，微博/知乎两项继续 blocked，整体保留进行中。
- 历史简短“突然不能浇水”反馈保留原归档和“无法唯一还原当次故障”的说明，不把本轮回归说成已查明当次唯一原因。

本轮合计 **6 次进度更新、4 次完成操作**，作者通知随原操作事务生成。状态目录为 14 条已完成、1 条进行中，仍不等于所有外部依赖或历史现场都已解决。
