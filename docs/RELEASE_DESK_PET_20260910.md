# 免费工位搭子：2026-09-10 发布

## 已上线结果

- 最终应用 **`48407006c70c5964c13ffd15e2670a322664344e`**，北京时间 **2026-09-10 14:35:41** 上线，已正常推送 GitHub `main`。发布记录等后续文档提交不重建应用。
- 初版 `754dcaf083b3841a7e0dbc77e92bd1b6c0b79cc4` 于 14:24:58 上线；末轮巡查发现直接刷新独立工具页没有账号引导，随即补齐并重新执行候选、备份、浏览器与公网门禁，不把初版当作最终完成。
- API 镜像：`sha256:3d761b607d2b64dd7965374a84bc1a5b99bab3965a201e49ac2aed7173f778b0`。
- Web 镜像：`sha256:46cd1fe839f03d882c6da526b22feac2914ba015444226ef07941c0c1e32559a`。
- 仅切换 API/Web，PostgreSQL、Redis、Gateway 容器 ID 不变；完整环境差分只有 `IMAGE_TAG`。schema36 / **37 条迁移、148 张 public 表**保持不变，生产没有执行迁移。

## 功能与边界

桌面侧栏及手机首页「工位搭子」，或 `/desk-pet`。默认不显示，主动领养后可拖动、摸头、免费喂食、睡觉/叫醒、方向键移动、Esc 收起、归位或关闭。三个内置矢量形象、四种视觉效果；用户自己的 PNG/JPG/静态 WebP 可在浏览器缩绘成 256px 贴纸与 32px 像素版。黑白纸片和拍立得均为本机样式，不声称 AI 重绘或自动抠图。

免费，不要求期权持有者，不改变办公室经济、VIP 兼容协议或管理权限。默认游戏/聊天专注避让，无声，支持安静和系统减少动画偏好。摸鱼境界祝贺仅消费已有事件，不额外授予经验或自动佩戴称号。

图片不上传，不保存原图/文件名/拍摄信息，不接受外部 URL 或 SVG。游客与账号按本机键隔离，切号移除旧图片；独立工具页冷启动也恢复正确身份。禁止存储、配额不足有明确提示；清除仅影响二次确认的当前桌宠键。**不跨设备同步，本机数据并非加密保险箱**。详见[使用、图片限制与隐私说明](DESK_PET.md)。

## 实际验收

- 最终前端 **166 个测试文件 / 1214 项通过**；受影响桌宠、路由与导航集中复跑 **74 项通过**。全仓类型检查及真正的 `VITE_SITE_MODE=community` 构建通过；既有大块产物提示仍存在。没有把未重跑的后端全量回归计入本次成绩。
- 最终不可变 API/Web 镜像在独立内部网络、全新合成 PostgreSQL 和两个普通测试账号下通过 Firefox 155.0.1 的 **8 组真实浏览器检查**：原生文件输入导入 PNG/JPEG/WebP、四风格及重绘尺寸、鼠标拖动不误触摸头、方向键/Esc、SPA 和工具页冷启动、睡觉与安静、真实登录/退出/切号隔离、拒绝 SVG 与确认清除。
- **13 张唯一截图及 SHA256** 校验通过；逐张确认截图像素宽度与 CSS 视口一致，包含真正 320px / 390px 页面和手机首页入口，无横向溢出。窗口只修改浏览器外壳的最小宽度，没有修改站点 CSS 来通过验收；它是响应式桌面 Firefox，不冒充真实手机设备测试。
- 测试代理只记录请求路径、字节计数及是否含图片特征，不保存密码请求正文；实际导入期间未观察到图片内容或桌宠上传请求。既有登录/刷新/全站请求不被说成“全站没有网络请求”。
- 每个发布目标都有新鲜限权加密备份，完整恢复到无外网克隆；最终候选逐个验证实体映射、无待执行迁移、148 表行摘要/列/索引/约束/触发器/序列及完整迁移历史不变。备份跨机器传输后逐项 SHA256 核验。
- 公网 **12 项通过**：实际 HTML、入口 JS、桌宠懒加载 JS 与候选逐字节哈希一致，健康与各静态入口正常，成长私有档案和支持管理接口匿名 401。线上没有使用真实成员账号导入图片或创建测试支持登记。
- 隔离登录会触发现有开户逻辑，两个普通合成账号办公币合计 1000（各 500）；支持与摸鱼记录均为 0。它们不是桌宠喂食奖励，不能把浏览器阶段写成“零资产变化”。

## 准备阶段发现及纠正

首次文件导入测试因浏览器 UID 无法读取限权测试目录而失败：仅另挂只读合成素材目录，不放宽真实备份或凭据权限。另修正了 harness 的嵌套引号与账号恢复重挂载造成的过期元素引用。

初次小窗口调用被 Firefox 限为 500px；截图复核后撤回该轮手机验收结论，增加严格 `innerWidth` 和 PNG 宽度断言，再完整重跑真正 320/390px 候选。失败尝试不作为发布凭据。最终应用另修复了工具页冷启动恢复、初次存储读取被拒提示，以及手机快捷入口换行。

GitHub 常规 SSH 推送出现断连/超时，HTTPS 无写入凭据。按 [GitHub 官方 443 SSH 说明](https://docs.github.com/en/authentication/troubleshooting-ssh/using-ssh-over-the-https-port)，核对[官方主机指纹](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/githubs-ssh-key-fingerprints)与已有 known_hosts 后，通过同一账户原密钥及严格主机校验完成正常快进推送；未强推、增加权限或修改全局 SSH/Git 配置。

## 回滚与留存

直接回滚到已保留镜像的 `754dcaf`，其后端认识 schema36，不需要关账号注销或回退迁移；只回 API/Web 并恢复当批环境，保留全部生产新记录及本机图片。要完全撤下桌宠，可另评审回到 `0b991be`，但也必须保留 schema36。此次没有执行回滚；回退应用同样可能中断内存中的临时 Paper 房间。

真实备份、环境、合成凭据和完整截图/测试日志限权留存，不进入 Git。测试数据库可由留存合成备份恢复，不能把它恢复到生产。

两轮隔离测试的数据库和日志均已跨机器复制、逐项核验 SHA256，再按核验过的标签、容器 ID 与挂载边界清理测试容器及独立网络。最终复核本轮测试资源无残留，生产五个服务运行正常；真实用户数据未删除。
