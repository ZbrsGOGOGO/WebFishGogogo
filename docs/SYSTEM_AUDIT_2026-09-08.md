# 摸摸公司系统级功能与安全审计记录 — 2026-09-08

## 1. 当前结论与报告状态

**阶段结论：已复现的问题已完成代码修复，三组新编译代码的隔离系统预演、全仓类型检查、后端/前端最终全量及社区构建通过；真实浏览器验收仍在补齐，不可变候选镜像验收及生产发布待执行。本文不是“网站已经全部修好并上线”的声明。**

本次按用户要求，对账号、权限、开发协作、附件、好友私聊、群聊实时消息、通知、办公币资产、种植、公会及游戏维护边界进行检查和修复。测试同时关注正常流程、异常输入、跨账号访问、重试、实际数据库并发、失败回滚和运行环境边界，不以“页面能打开”代替业务验收。

截至本版文档已确认：

| 验证层级 | 已确认结果 | 解释与限制 |
| --- | --- | --- |
| 账号与开发模块专项回归 | 19 套、156 项通过；该范围起始回归为 149 项，新增 7 项 | 含服务、控制器、守卫、附件和安全工具测试；不是 156 次完整浏览器端到端测试 |
| 账号/开发隔离预演 | `ACCOUNT_REHEARSAL_OK 9 system scenarios` | 真实 Nest HTTP、PostgreSQL、JWT/权限守卫与 Multipart；126 张表清理后指纹一致 |
| 资产经济隔离预演 | `ASSETS_REHEARSAL_OK {"scenarios":10,"syntheticUsers":27}` | 真实 PostgreSQL；含 8 路竞争、失败回滚、跨日与维护开关验证 |
| 社交实时隔离预演 | `result=passed`，18 组、3 个合成用户 | 两个 API 实例、真实 WebSocket 和隔离 Redis；数据库及 Redis 状态恢复 |
| 全仓最终类型检查 | shared/backend/frontend 全部通过 | 根代理执行完毕，已读取 `final-typecheck.log` 核对 |
| 前端最终全量 | 114 个文件、721 项通过，54.63 秒 | 已读取 `final-frontend.log`；真实浏览器验收及最终构建另列门禁 |
| 后端最终全量首轮 | 115 套通过、1 套失败；756 项通过、1 项失败 | 旧 reading 测试夹具未显式设为 active，补齐后正在重跑；此轮仍是失败，不能写成全量通过 |
| 后端冻结后全量复验 | 116 套、757 项全部通过，150.13 秒 | `final-backend-recheck.log`；仅补齐正常测试账号的 active 状态，未放松安全校验或断言 |
| 社区模式构建 | shared/backend/frontend 全部通过 | `build-community-preview.log`；正式生产配置的候选镜像仍需独立构建验收 |
| 运行时依赖公告审计 | 官方 npm registry：0 个已知漏洞 | `npm audit --omit=dev --registry=https://registry.npmjs.org --json`；不包含开发依赖，不等于业务无漏洞；最初配置的镜像源不支持审计，已换官方源复验 |
| 新演练脚本 | 三组均已实际运行；账号脚本另经语法和交叉安全审阅 | 不把“写好了脚本”视为“已经跑通” |

以上合计 **37 个系统检查组**。其中包含环境隔离和清理检查，一个组内可含多个 HTTP 请求或并发断言；该数字不代表 37 个独立页面，也不是漏洞覆盖率。

### 1.1 版本与环境界限

- 工作分支：`feat/workstation-tower-defense`。
- 本轮本地基线：`59d62d3d9a7df459b51f2111de4ff7676c24dc45`。
- 根代理记录的本轮生产起始基线：`0ec2d0b`；它不是本报告修复后的发布版本。
- 本报告记录的三组预演：使用 `0ec2d0b` 容器的已有依赖，**只读挂载本轮新编译的 backend/shared 代码**。尚非最终不可变候选镜像，不能代替候选镜像重跑。
- 隔离数据库分别为 `community_account_rehearsal`、`community_assets_rehearsal`、`community_social_rehearsal`；浏览器系统测试使用另一独立库，避免互相污染。
- 三组预演均使用合成数据，数据库结构至迁移 `0029`。当前生产也已为 `0029`；本次发布应是 **29 → 29、零新增迁移**，不是重放此前 Rail/密码功能的 28 → 29 升级，也不是回退迁移至 28。
- 本文不记录任何生产密码、刷新凭证、环境文件、数据库备份、真实用户消息或私有附件内容。

### 1.2 当前线上开放范围与隔离测试开关不同

根代理对生产功能开关进行了只读确认。本轮**不改变任何功能开关**，不能因为隔离测试显式开启某功能，就在发布说明中宣称它已经对线上用户开放。

| 生产状态 | 功能范围 | 本报告中的含义 |
| --- | --- | --- |
| 已开启 | 社区写入总开关、聊天室、聊天写入、开发协作、账号注册 | 本轮相关修复具有当前生产使用场景，仍需最终镜像和线上验收 |
| 已开启 | 社区内容读取、新闻读取、新闻自动刷新 | 可以读取/更新既定新闻快照；不等于用户发帖或管理员新闻发布已开放 |
| 未开启 | 社区内容写入、社区审核、新闻管理发布 | 相关实现可以回归测试，但本轮没有开放生产发帖、审核或新闻发布 |
| 未开启 | 密码找回、账号注销、社交验证、推荐邀请、旧办公战斗功能 | A05、A06 等在隔离库开启开关以验证实现；不等于生产已有找回、注销或外部认证可用入口 |
| 已开启的运行能力 | 邮件 outbox 后台泵、内置聊天审核 | 开关状态不证明真实邮件送达或所有外部提供商已验证；隔离预演明确关闭真实发送与后台泵 |

旧签到/Platform 控制器和旧 office-battle 入口未挂载到当前社区生产。对共享服务的缺陷修复和测试，不应写成“已发现线上可直接调用旧签到刷币”等未证实结论。

## 2. 方法与安全约束

### 2.1 测试方法

1. 阅读实际入口、服务事务、数据库锁、权限校验和私有数据投影；先提出可验证的故障条件，再修复。
2. 为已发现问题增加回归测试。账号刷新新增用例在原实现上失败后修复；DOCX 使用相同合成文件对比原版本与修复版本的实际检查入口。
3. 在隔离 PostgreSQL 中制造真实事务等待，用 `pg_blocking_pids` 验证阻塞链，不仅依赖延时或 mock 调用顺序。
4. 用 `FOR UPDATE NOWAIT` 探针确认等待账号锁时，没有提前持有会话/刷新凭证锁；同时检查最终会话、凭证、余额和流水状态。
5. 对账号、开发、好友、私聊、通知等通过真实 HTTP 控制器、守卫和解析链路操作；实时社交通过两个实例及真实 Redis/WebSocket 验证。
6. 在本地或隔离环境注入外发事件、通知写入失败，检查业务数据是否整体回滚，以及重试是否只结算一次。
7. 完成后删除精确识别的合成记录，恢复被测试改动的种子配置，用全表行数及完整行内容指纹比较检查是否残留。

### 2.2 环境硬闸

- 演练脚本校验明确的确认标识、固定测试主机/数据库名、专用连接配置，拒绝生产数据库连接字符串及不合适的运行模式。
- 只允许空业务测试库及已知迁移种子数据；不是对生产数据库执行“测试后再删掉”。
- **隔离演练中的**邮件后台泵和真实邮件发送未启用。密码重置仍验证真实加密入队和原子消费，不把“不发送邮件”误写成“邮件已送达”，也不与生产泵开关状态混淆。
- 不使用真实账号模拟管理员，不修改生产角色，不读取真实附件、私聊或验证码。
- 不在生产发送恶意 DOCX、压缩包、越权探针、暴力密码尝试或并发攻击负载。
- 账号清理保留 PostgreSQL 时间戳微秒精度，避免用 JavaScript 毫秒时间回写后掩盖或造成指纹差异；没有使用全库清空替代精确清理。

## 3. 账号与开发协作：9 组实际系统场景

实现入口：[community-account-rehearsal.cjs](../deploy/community-account-rehearsal.cjs)。以下均已由根代理实际执行预演，并独立核对成功日志。

| 编号 | 场景与操作 | 验证的结果 | 本轮结果 |
| --- | --- | --- | --- |
| A01 | 校验环境硬闸、迁移至 `0029` 并重跑迁移；启动精简真实 Nest HTTP 栈 | 空业务库；迁移重跑无新增；使用真实控制器、JWT/开发守卫、Multipart；不运行真实邮件或后台业务泵 | 通过 |
| A02 | HTTP 注册、同名大小写重试、登录、刷新、错误来源、错误/正确当前密码、多个设备、他人设备撤销、重复退出 | 注册保存四份同意记录；规范化账号唯一；刷新凭证只进 HttpOnly、SameSite=Strict Cookie，不进 JSON；不可信 Origin 被拒；旧密码不再登录；改密后旧 JWT/刷新凭证失效；只能撤销自己设备；退出幂等 | 通过 |
| A03 | 持有真实用户行锁，同时启动刷新与单设备退出、全部退出、设备撤销、改密，四组竞争分别执行 | 确认两条业务操作实际排队；间接阻塞链也被计入；NOWAIT 探针确认未提前占会话/凭证锁；撤销操作成功，刷新至多在撤销前成功，最终不存在活跃刷新凭证，旧 JWT 被拒 | 通过 |
| A04 | 刷新请求开始时凭证有效，等待用户锁时推进合成时钟超过会话与凭证期限 | 锁释放后返回 `INVALID_REFRESH_TOKEN`；不新增刷新凭证、不延长会话；会话撤销原因为 `expired` | 通过 |
| A05 | 密码重置请求加密入队；等待用户锁期间重置凭证过期；再创建有效凭证并由两个真实事务同时消费 | 入队不保存明文邮箱/凭证；过期消费不改密码、不撤销原有效会话；同一有效凭证只有一个消费成功；密码修改后旧会话不可用；不存在账号的请求保持相同返回契约 | 通过 |
| A06 | 刷新另一个设备的同时申请账号注销；重放注销请求；取消冷静期注销 | 账号进入 `deleting`，其他设备撤销，普通接口不可访问；相同请求幂等；冷静期可取消并恢复原账号状态；未运行真实用户最终匿名化 | 通过 |
| A07 | 通过真实 Multipart 上传约 80KiB 未闭合正文节点的合成 DOCX，随后调用鉴权接口和 `/health/ready` | 返回 `DEVELOPMENT_ATTACHMENT_INVALID` 且错误指向正文节点；在脚本 3 秒上限内拒绝；未增加附件或改变提案版本；后续 API 和数据库就绪接口仍响应 | 通过 |
| A08 | 合成管理员授予普通账号开发协作；作者/他人/管理员读写提案；并发同版本评论与决策；真实上传/下载；撤销协作权限 | 开发协作不提升站点角色；普通协作者不能授权他人或做管理员决策；他人提案/附件拒绝访问；同版本并发只有一个成功；非作者管理员也不能代作者上传；HTML 与超 5MiB 文件拒绝；下载权限和安全头正确；撤权后立即拒绝后续请求 | 通过 |
| A09 | 关闭测试 HTTP 栈；按新增主键清理合成记录；恢复容量锁种子行；比较全库指纹 | **126 张表**的行数和完整行内容指纹均回到测试前状态，无测试账号、附件、通知、审计记录或限速桶残留 | 通过 |

### 3.1 A08 的权限与附件细节

真实演练区分四种身份：合成站点管理员、作者协作者、另一协作者、无协作权限的普通账号。

- 开发授权写入独立协作资格；作者账号的 `communityRole` 仍为 `user`。`AccountAdminGuard` 使用真实数据库分别验证普通账号拒绝和管理员允许；这部分不是对所有管理员 HTTP 接口的完整测试。
- 无权限访问开发接口为 403；另一协作者读取作者提案或附件为 404，避免泄露可访问记录的存在性。
- 提案创建相同意图重放返回同一提案；评论/状态改变使用 `expectedVersion`，真实并发的另一请求返回 `DEVELOPMENT_VERSION_CONFLICT`，不丢失或覆盖已提交内容。
- 文件上传先经过作者守卫再进入 Multipart 处理；管理员可以审核下载，但不能替作者消耗上传额度。
- 真实上传覆盖 UTF-8 中文文件名、正常文本、主动 HTML 后缀、超过 5MiB 的文件、畸形 DOCX。ZIP 条目越界、压缩比例、数量、伪造大小、加密、DOCX DTD/宏等另有附件专项回归，不把这些全部算成已做的 HTTP 攻击场景。
- 私有二进制不在普通实体查询中返回。下载设置 `application/octet-stream`、`Content-Disposition: attachment`、`private, no-store`、`nosniff`、`Cross-Origin-Resource-Policy: same-origin`。
- 审核导出明确提示附件/提案正文是待审数据，不是系统指令；不因附件内容自动执行命令或修改网站。
- 撤销协作资格后，原账号后续提案提交及附件读取都被权限校验拒绝。

## 4. 其他真实系统预演

### 4.1 资产、种植、公会和维护边界：10 组

入口：[community-assets-rehearsal.cjs](../deploy/community-assets-rehearsal.cjs)，27 个合成账号，预演通过。

| 编号 | 检查组 | 核心断言 |
| --- | --- | --- |
| E01 | 8 路初始化和签到 | 初始资产只建立一次；签到只发一次；回执、奖励和 outbox 数量一致 |
| E02 | 上海零点签到 | 新自然日只有一份新签到奖励，不把同日重试当新一天 |
| E03 | 钱包重试、业务意图冲突和余额竞争 | 相同幂等键只应用一次；换来源/原因拒绝；8 路大额扣款只有余额允许的一次成功，不透支 |
| E04 | 背包幂等 | 同一次发物品只落一份流水；相同键换业务来源被拒，数量准确 |
| E05 | 奖励排队期间账号停用 | 实际用户锁等待后复查状态，停用账号不建立资产、不获得签到奖励 |
| E06 | 外发事件失败和 Arcade 维护开关 | outbox 写后失败使余额、经验、奖励、签到全部回滚；重试一次成功；社区模式禁止新开局/结算且全表不变，公共榜单仍可读；旧完整应用模式行为未被改成社区模式 |
| E07 | 收获失败、8 路重试和前三单之后的收益 | 通知失败完整回滚；同次收获只结算一次；第 4、5 次合成收获仍按当前规则有基础奖励，不是“只前三次有币” |
| E08 | 公会捐献、Boss 重试和换公会 | 同次捐献只扣一次；同次 Boss 奖励只发一次；同服务日换公会不能再次领取个人 Boss 奖励；上海 05:00 服务日切换后可获得下一日机会 |
| E09 | 公会榜单前 20 名边界 | 人工构造明确标注的持久化边界：本人不在展示前 20 名时，个人贡献和已用次数仍正确；不声称正常战斗一定达到该人数状态 |
| E10 | 清理与全表指纹 | 仅删除精确匹配的合成用户、公会和测试物品；全表指纹恢复 |

### 4.2 好友、群聊、私聊与通知：18 组

入口：[community-social-rehearsal.cjs](../deploy/community-social-rehearsal.cjs)，两个 API 实例、真实隔离 Redis DB 3、3 个合成账号，预演通过。

| 编号 | 检查组 | 核心断言 |
| --- | --- | --- |
| S01 | 隔离环境 | 空业务 PostgreSQL、迁移 `0029`、专用空 Redis；无生产数据 |
| S02 | HTTP 登录边界 | 未登录不能访问好友、群聊、私聊、通知；正常账号可读开放聊天室 |
| S03 | 群聊发送与停用竞争 | 实际阻塞链确认发送等房间锁、停用等用户锁；发送先提交则停用在其后，停用后新发送被拒，不再在已完成停用后插入排队消息 |
| S04 | 好友申请 | 两实例重试幂等；非接收者不能接受；正确接收者只建立一份好友关系 |
| S05 | 私聊会话所有权 | 同一好友对只有一个规范化会话；无关账号不能读取或推进已读位置 |
| S06 | Redis 跨实例路由 | 实例 A 发出的真实 WebSocket 私聊到达实例 B 的收件人，无关账号不收到 |
| S07–S12 | 撤销/过期 × 群聊/私聊/已读事件，共 6 组 | 静默旧连接无需主动发消息，在心跳前的广播阶段就被核验并以 4401 关闭；不收到该事件；同账号仍有效的设备正常收到；旧 HTTP Token 同样被拒 |
| S13 | 私聊重试与撤回 | 同请求只有一个序号；改内容重试冲突；他人不能撤回；撤回结果跨 Redis 更新 |
| S14 | 历史、重连和已读游标 | 新旧分页无遗漏/重复；前向游标有序；错误游标和分页限制拒绝；已读后未读数归零 |
| S15 | 删除好友、拉黑、解除拉黑 | 删除好友保留受权限保护的旧会话但不能继续发送；任一方向拉黑都隐藏私聊；解除拉黑不会自动恢复好友关系 |
| S16–S17 | 全部已读/农场分类已读，共 2 组 | 仅标记已送达且可见通知；未来、已过期和被屏蔽来源的通知保持未读；未来通知到达时仍出现；别人的通知不能标记 |
| S18 | 精确清理 | 合成社交数据删除，聊天室种子状态恢复，全部表指纹及 Redis 状态恢复 |

## 5. 已确认问题、修复和证据边界

下表为本轮实际代码与回归所确认的问题，不是自动扫描的可能性清单，也不代表生产已被利用。没有根据生产事件日志声称账号或资金曾被盗。

| 编号 | 已确认问题及影响 | 修复方式 | 已有证据 / 尚不能推出的结论 |
| --- | --- | --- | --- |
| F01 | 刷新/退出原先从凭证、会话取锁，与改密/重置/注销的账号优先顺序不一致，存在数据库互等条件，影响会话操作稳定性 | 先查能力归属，再统一账号 → 会话 → 刷新凭证；锁后重新确认归属及哈希 | 新增原实现失败的锁序回归；A03、A06 实际 PostgreSQL 排队及最终状态通过；没有宣称捕获过生产死锁事故 |
| F02 | 刷新/重置凭证用等待锁前的时间判断有效性，可能在真正处理时已经过期仍被消费 | 使用获得必要锁后的时钟检查有效期；重置消费仍由数据库原子条件保证一次性 | 原刷新到期回归实际失败；A04、A05 真实锁等待跨过到期后拒绝，密码/会话/凭证状态符合预期 |
| F03 | 社交验证回调、过期、提供商失败收尾与账号清理锁序不一致；回调原先没有要求锁后账号仍可用 | 三条涉及账号写入的路径改为账号优先；回调及收尾复查账号状态；回调期限使用处理时钟 | 专项回归验证顺序及注销中账号拒绝；未调用真实实名认证提供商，不计作第三方链路上线验收 |
| F04 | DOCX 纯文本提取的全局“寻找闭合文本标签”正则对重复未闭合节点产生二次扫描，异步 ZIP 超时不能中断同步事件循环 | 单向扫描标签；拒绝未闭合/嵌套文本节点；保留实体、属性、空节点、Tab、换行回归 | 同一 80,491 字节合成 stored DOCX，原实际入口接受并耗时约 124ms，新版约 2ms 拒绝；A07 Multipart 通过。测量是单次本地观察，不是性能 SLA；利用前提仍为有上传权限的作者，不是匿名上传入口 |
| F05 | 已撤销/过期的静默 WebSocket 可能在下一轮心跳前仍收到广播 | 广播前批量复查收件连接的服务端会话和账号，失败关闭连接 | S07–S12 六种实际 Redis/WS 组合通过；不把“广播前复查”描述为撤销事务提交与网络发送之间绝对零时隙的形式化保证 |
| F06 | 群聊发送只读取未锁账号，等待房间锁期间账号可先被停用/注销，随后旧请求仍插入消息 | 账号 `FOR NO KEY UPDATE` → 房间锁，与生命周期顺序一致，保持账号有效至发送事务提交 | S03 用真实 PostgreSQL 房间锁与用户锁竞争确认提交顺序；非生产攻击压测 |
| F07 | “全部已读”可能提前标记未来的农场成熟提醒，也未完整沿用可见时窗/屏蔽规则 | 仅更新已可见、未过期且未被关系屏蔽的通知 | S16–S17 实际 HTTP 及数据库状态通过 |
| F08 | 资产共享服务仅确认账号存在，未在锁后确认 `active`，会接纳调用期间已停用账号 | 初始化及资产处理复查账号状态，失败不落奖励资产 | E05 实际排队期间停用后无新增奖励；属于共享层防御，旧签到控制器未挂载社区，不宣称线上存在可直接调用的签到刷币入口 |
| F09 | 钱包/背包相同幂等键的重放只核对账号、资产和数量，未完整核对业务来源和原因 | 增加来源类型、来源 ID、原因一致性比较，冲突返回幂等键复用错误 | E03、E04 验证合法重试只结算一次、变更意图被拒；原缺口不等于已经证明可重复发币 |
| F10 | 公会 Boss 机会按所在场次判断，换公会可能重新获得当日奖励；本人贡献取自前 20 名展示也会误判个人次数 | 以账号和服务日查询跨公会已用机会；本人贡献独立于排行榜展示查询 | E08、E09 实际持久化及边界验证通过，保留服务日 05:00 规则 |
| F11 | 社区 Arcade 开局/结算未遵守社区维护写开关 | 仅在显式社区模式下，入口与事务账号锁后均检查开关；榜单保留可读 | E06 验证维护期间全表不变；旧完整应用模式与旧版回退镜像不因此自动成为只读 |
| F12 | 登录返回目标丢失查询/锚点，Rail 密码输入的 UTF-16 限制与后端 Unicode 字符限制不一致，顶层渲染异常缺少恢复界面 | 保留受控站内目标，调整输入边界，增加不暴露原始异常内容的恢复界面 | 对应新增失败用例、修复后 4 套/33 项专题及最终前端 114 文件/721 项通过；根代理已在真实浏览器复现原登录丢查询问题，修复版本完整浏览器复验仍待第 8 节补齐 |

不将尚未验证的特殊管理员交叉操作等并发推测列为漏洞或“已修复项”。本轮没有据此扩展生产权限或调整真实账号。

## 6. 全站模块测试矩阵

“专题通过”只表示对应证据范围；“待补”不能在发布结论中改写为通过。最终全量和浏览器结果应由执行发布的根代理补入。

| 模块/入口 | 已覆盖的重点 | 当前证据 | 浏览器/最终镜像待验收 |
| --- | --- | --- | --- |
| 注册、登录、Cookie、刷新 | 同意记录、账号唯一、来源限制、私有凭证、有效期、撤销 | A01–A05；账号专项回归 | 表单提示、保留目标、不同浏览器 Cookie、候选镜像重跑 |
| 设备、改密、账号生命周期 | 跨账号设备隔离、全设备撤销、删除冷静期与取消 | A02、A03、A06；生命周期专项回归 | 设置页流程；最终匿名化/长期定时处理不由 A06 代替 |
| 社交认证、邮件找回 | 回调签名/重放与状态回归、加密 outbox、一次性找回 | 专项回归、A05 | 真实邮件收件/退信、外部认证提供商均未测试 |
| 个人资料、隐私、推荐邀请 | 现有服务与前端测试覆盖 | 已有测试；最终冻结全量待补 | 实际页面和隐私设置传播待补 |
| 开发协作入口/管理权限 | 开发角色与站点管理员分离、授权撤销、私有记录 | A08 与开发专项回归 | 菜单可见性、管理员/协作者页面操作待补 |
| 提案、评论、审核、附件 | 幂等、版本竞争、下载权限、危险格式和大小 | A07、A08；附件/控制器专项回归 | 拖拽/错误提示/下载文件名；最终候选重跑 |
| 好友、私聊 | 好友申请、会话隔离、拉黑、撤回、分页、已读 | S04–S06、S13–S15 | 多页面切换、重连显示与移动布局待补 |
| 群聊、实时传输 | 跨实例、撤销静默连接、发送与停用竞争 | S02、S03、S06–S12 | 聊天页面滚动、切换、发送失败反馈待补 |
| 通知中心/成熟提醒 | 可见时窗、未读、分类已读、他人权限 | S16–S17 | 角标/通知跳转待补 |
| 钱包、背包、签到、奖励 | 一次性、业务意图、并发余额、失败回滚、账号状态 | E01–E06 | 办公币展示和页面刷新待补；旧签到入口不因测试而开放 |
| 桌面种植 | 成熟收获、重试、通知失败回滚、前三单后基础收益 | E07 | 生长时间、余额更新、订单展示待补 |
| 公会 | 捐献、Boss 奖励、服务日、换公会、个人次数 | E08–E09 | 状态展示与榜单页待补 |
| 工位塔防/旧办公战斗代码 | 既有引擎、关卡、成长、门禁等测试；旧办公战斗生产开关关闭且旧入口未挂载 | 已有测试；最终冻结全量待补 | 以当前实际暴露页面为准，不宣称旧服务上线；本轮非美术重做 |
| Arcade 单机/遮司 | 社区维护写闸；既有积分与游戏逻辑回归 | E06 与 Arcade 专项 | 全部单机游戏完整游玩未被 E06 覆盖；上线镜像榜单与提交待补 |
| Play 玩家房间 | 既有房间、密码、引擎、积分奖励测试 | 现有回归；之前发布验收不计入本轮新预演 | 本轮候选真实房间预演/页面待补 |
| Rail《轨道难题》 | 既有引擎/私有手牌/房间/奖励；Unicode 输入修复 | 现有回归及前端专题；不借用旧发布结果冒充本轮结果 | 本轮候选 Rail 预演、建房/旁观页面待补 |
| 各类排行榜 | 经济个人状态、公会展示边界，Arcade 维护时公共榜可读 | E06、E09；既有 Play/Rail 奖励测试 | 专区切换、本人名次、最终日奖作业待补 |
| 热点新闻/榜单快照 | 现有分类、快照、抓取安全与发布门禁测试 | 已有回归；最终全量待补 | 当日内容展示、外部来源可用性/降级待补 |
| 社区内容、动态、举报/审核 | 既有内容与权限回归 | 最终冻结全量待补；早期全量有失败，不能据此标记通过 | 实际页面、管理员流程待补 |
| 页面异常恢复、导航、移动端 | 新增恢复边界、登录站内目标、Unicode 密码回归 | 前端专题 4 套/33 项及最终全量 114 文件/721 项通过 | 全路由、窄屏、浏览器控制台与真正渲染异常待补 |
| 健康检查、路由白名单、维护模式 | A07 就绪恢复、社区 Arcade 写闸、白名单代码审阅 | A07、E06；模块边界回归 | 最终候选和线上 health/ready、禁止路由检查待补 |
| 依赖、构建、备份与发布 | 根代理已建立扫描和发布验证流程 | 最终证据待第 8 节补入 | 不可变镜像、完整备份校验、回滚、GitHub/线上版本对齐待补 |

### 6.1 旧应用入口不是当前社区公开功能

[CommunityAppModule](../packages/backend/src/community-app.module.ts) 是社区白名单入口，未导入旧 `AppModule` 的 documents、reading、memo、tools、旧 farm/arena 等模块。旧签到所属 `PlatformModule` 也没有因本轮测试而加入社区入口。

因此：

- 对旧农场、旧竞技场、任务、签到服务修补测试夹具或验证内部资产函数，不等于向社区用户开放这些旧接口。
- 对旧模块运行测试可防共享底层回归，但不能拿它代替当前网站实际路由验收。
- Arcade 同时被社区模式和旧完整应用使用；本轮维护开关修复仅明确约束社区模式。
- **不能仅凭社区写开关就声称全站只读，更不能认为旧版回退镜像包含本次修复。** 发布维护/回滚时必须按实际暴露入口、镜像版本和路由白名单验证。
- 当前回滚基线 `0ec2d0b` 已支持房间密码，但不包含本次 Arcade 维护开关修复。不能套用旧版本的“密码盲回滚”迁移说明；也不能因为它已支持密码就假定维护写闸完整。

## 7. 已知风险边界与未验证项

1. **外部提供商**：未对真实邮件投递、退信、第三方社交认证/实名认证或外部审核服务进行端到端验收。内部加密队列、签名解析和适配器回归不证明真实服务的网络与凭证配置可用。
2. **真实用户数据**：未读取真实私有附件或聊天记录，不用真实管理员号做授权实验，未运行真实账号最终删除/匿名化；A06 验证的是合成账号冷静期流程。
3. **生产攻击测试**：没有向生产进行畸形文件、暴力猜密码、并发刷奖励、越权请求或持续压力测试。本轮安全用例限隔离环境。
4. **生产负载与性能**：受控 8 路竞争和单次 DOCX 时延并非容量测试、长时浸泡、网络抖动测试或 SLA。广播前的会话复查会增加数据库查询，需在后续真实容量预算中观察。
5. **形式化安全保证**：没有证明所有可能线程交错、所有角色组合、所有旧接口和所有文件格式都无漏洞；不存在“本次通过所以今后无需再测”的结论。
6. **浏览器与终端**：在第 8 节填写结果前，不宣称 Firefox/Chromium、移动端、复杂输入法和断网重连全部完成验收。
7. **时钟与真实日奖作业**：合成时钟验证边界规则，不等于已等待真实午夜/05:00 作业在生产执行。需要线上只读观察与运行日志补证。
8. **构建产物一致性**：本次 37 组是挂载编译产物预演；最终镜像还必须重跑相关门禁，避免依赖、构建、配置或入口差异。
9. **报告更新纪律**：任何失败必须保留并解释修复或重跑结果；不能只挑通过日志，也不能把发布前尚未执行的步骤写成完成。

## 8. 最终全量、浏览器与发布门禁（待根代理补充）

以下表格特意保留未完成状态。应填入实际退出码、数量、日志或产物标识，不以“预计正常”结项。

| 门禁 | 当前状态 | 完成后应补的信息 |
| --- | --- | --- |
| 冻结代码完整性 | 待最终确认 | 最终提交 SHA、分支、工作区状态、修改范围 |
| 全仓最终类型检查 | 通过 | `final-typecheck.log`；shared/backend/frontend 三个 workspace，根代理确认成功 |
| 后端最终串行全量测试 | 首轮有 1 项失败，修正测试夹具后重跑中；尚不能结项 | `final-backend.log` 为 116 套/757 项中的 1 项 reading fixture 失败；最终填写 `final-backend-recheck.log` 的套数、总数、失败/跳过和退出码 |
| 前端最终全量测试及构建 | 测试通过，构建待补 | `final-frontend.log`：114 文件、721 项、54.63 秒；社区构建正在执行，结果另补 |
| 依赖审计与处置 | 待补 | 扫描来源、生产/开发依赖区分、发现与处置/残余风险 |
| 真实浏览器系统流程 | 待补 | 浏览器/视口、页面清单、账号角色、步骤、截图或日志、失败后复验 |
| 不可变候选镜像构建 | 待补 | 镜像 tag/digest、对应源码 SHA、依赖及编译一致性 |
| 候选镜像真实 PG/Redis 门禁 | 待补 | account/assets/social 重跑结果及其它本轮需覆盖的 Play/Rail/news 结果；不直接引用 preview 作为 candidate 成功 |
| 备份与恢复校验 | 待补 | 备份完整性、异机副本、恢复/指纹验证、明确回滚版本；文档不得写密钥或备份内容 |
| Git 提交与 GitHub 同步 | 待补 | 提交 SHA、目标分支、推送成功结果；不得默认已推送 |
| 生产部署 | 待补 | 部署时间、实际运行镜像/提交、迁移状态、容器健康；不得默认已生效 |
| 线上只读验收 | 待补 | health/ready、首页/关键路由、静态资源、禁止入口、版本一致性；禁止把生产冒烟扩成攻击测试 |
| 发布后结论 | 待补 | 分别写明“代码已修复”“GitHub 已同步”“网站已生效”，失败时明确停在哪一层 |

早期全量日志是诊断证据，不是最终发布证据：`backend-baseline.log` 记录过 3 套失败、112 套通过；该阶段存在新增回归与开发中修改，应由冻结后全量逐项确认，不能因其他专项通过就忽略。后端 `final-backend.log` 的剩余 1 项失败同样保留，等待补齐夹具后的全量重跑。前端起始 112 套/707 项、专题 4 套/33 项和最终 114 文件/721 项是不同轮次，不能相加成测试总数。

## 9. 证据索引与复现约定

### 9.1 已独立读取核对的隔离预演日志

日志位于服务器专用审计目录，未提交仓库；路径用于操作员查证，不是公网下载链接。

| 日志 | 完成标识 |
| --- | --- |
| `/opt/webfish-backups/full-system-audit-20260908.4rbx3t/preview-account.log` | `ACCOUNT_REHEARSAL_OK 9 system scenarios`；126 表指纹恢复 |
| `/opt/webfish-backups/full-system-audit-20260908.4rbx3t/preview-assets.log` | `ASSETS_REHEARSAL_OK`；10 组，27 合成用户；全表指纹恢复 |
| `/opt/webfish-backups/full-system-audit-20260908.4rbx3t/preview-social.log` | JSON `result=passed`；18 组，3 合成用户，`productionDataRead=false`；PG/Redis 恢复 |

本地阶段日志目录：`/tmp/webfish-full-audit.bmEyRd`。已用于核对的非敏感日志包括 `backend-baseline.log`、`frontend-baseline.log`、`frontend-fixes.log`、`login-red.log`、`frontend-red.log`、`error-boundary-red.log`、`final-typecheck.log`、`final-frontend.log`、`final-backend.log`。`final-backend-recheck.log` 的最终结果仍待补入。该目录其他文件可能属于运行凭据或环境资料，**不应整体复制、提交或附在公开报告中**。

### 9.2 可复核的仓库入口

- 系统演练：[账号与开发](../deploy/community-account-rehearsal.cjs)、[资产经济](../deploy/community-assets-rehearsal.cjs)、[社交实时](../deploy/community-social-rehearsal.cjs)。
- 账号专题：[AuthService 回归](../packages/backend/src/modules/auth/auth.service.spec.ts)、[密码重置回归](../packages/backend/src/modules/auth/password-reset.service.spec.ts)、[社交验证回归](../packages/backend/src/modules/auth/social-verification.service.spec.ts)。
- 开发专题：[开发服务回归](../packages/backend/src/modules/development/development.service.spec.ts)、[附件策略回归](../packages/backend/src/modules/development/development-attachment-policy.spec.ts)、[控制器回归](../packages/backend/src/modules/development/development.controller.spec.ts)。
- 生产入口白名单：[CommunityAppModule](../packages/backend/src/community-app.module.ts)。

本地非生产专项复核命令：

```sh
npm test --workspace @stealth-reader/backend -- --runInBand modules/auth modules/development
npm run typecheck --workspace @stealth-reader/backend
node --check deploy/community-account-rehearsal.cjs
node --check deploy/community-assets-rehearsal.cjs
node --check deploy/community-social-rehearsal.cjs
```

真实 PostgreSQL/Redis 预演只能在专用隔离环境按脚本硬闸重新建立；不得把生产 `.env` 或生产库名代入命令。若硬闸拒绝、清理指纹不一致或测试失败，停止相应发布步骤并处理，不能移除硬闸让测试“通过”。
