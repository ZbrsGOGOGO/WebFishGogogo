# 开发协作免费 AI 发布记录（2026-09-20）

状态：**应用代码已推送 GitHub `main`，API/Web 已部署；免费 AI 能力保持关闭，等待站长配置 Groq 免费密钥后再单独启用。** 应用提交为 `2381528a50a9a3de013d3907a2427501fa00a2a7`，北京时间 2026-09-20 11:18 完成切换。

## 运行版本

| 对象 | 提交 / 镜像 |
| --- | --- |
| API | `2381528a50a9a3de013d3907a2427501fa00a2a7` / `sha256:34fad6d3c1c6f8d2824ea88297814ce9f8817bbdfb3d72e13962e52c46038041` |
| Web | `2381528a50a9a3de013d3907a2427501fa00a2a7` / `sha256:9c0f42071e97918953dbf245ddcc63a66a5a2587715a625ab693cc372dbbbba0` |

数据库保持 schema41、42 条迁移、151 张 public 表。本批无迁移，不改变办公币、权限、提案状态或附件。PostgreSQL、Redis、Gateway 容器身份和数据卷未变；仅切换 API/Web。API 重启会结束内存临时房间，这是既有运行边界。

## 功能与边界

- 开发协作提案详情新增免费 AI 讨论卡片，固定使用 Groq 免费层 `openai/gpt-oss-20b`，不配置付费模型或自动回退。
- 服务端保管密钥；前端、响应和仓库不包含密钥。只有原开发协作成员可访问，并继续复用提案级隔离，普通成员不能借 AI 读取他人提案。
- 用户每轮必须明确勾选知情；只发送当前提案正文、最近人工评论和本轮聊天。附件正文、文件、账号私密字段不发送。
- 回答只保留在当前浏览器页面，不自动写入正式时间线；用户可手动将一条建议填入评论再确认发送。AI 不能更改状态、提交 Git 或触发部署。
- 站内保护为每账号每天 20 次、全站每天 200 次；Groq 免费限额返回后直接停止，不切换模型。
- 当前生产 `FEATURE_DEVELOPMENT_AI_ENABLED=false` 且 `GROQ_API_KEY` 为空，因此入口对成员隐藏，不能把本次发布描述为“免费模型已经可聊天”。启用前须由站长创建免费项目密钥，写入限权环境文件并通过预检，再只重新创建 API 容器；密钥不得通过聊天或 Git 提交。

## 验证

- 全仓类型检查和生产构建通过。
- 后端 168 个测试套件、1655 项通过，另 85 项按既有外部条件跳过；前端 224 个测试文件、1956 项全部通过。
- 新增验证覆盖提案级权限、明确知情、严格输入、提示词注入边界、附件不外发、返回纯文本、免费额度耗尽无付费回退、前端临时聊天和部署默认关闭。
- 上线后本机健康、API ready、数据库迁移/表数量、镜像 ID、限权环境文件和静态 UI 文案均通过。公网首轮在文字战线不存在房间检查处发生一次 TLS 握手超时，随后本机来源受短时连接限制；从生产主机对公网完整重跑 67 项 HTTP 检查全部通过。认证 Cookie 烟测因未配置专用合成凭据而明确跳过。

## 备份与回退

- 发布前限权备份位于 `/opt/webfish-backups/development-ai-20260920.bYdzCW`，包含环境、容器与卷清单、PostgreSQL dump、Redis RDB、仓库 bundle 和 SHA-256 清单；逐项校验通过。
- 普通应用回退恢复 API `590db0b3ec08f1fb2bdec5e48e0f8371e6ac3120` 镜像 `sha256:ba44dd5fda93418c85381e44f40f615a00c0d872a8a39649e7f4ea583ef3b3bc`，以及 Web `24a7baea8f418d0b36553f4bb532b8e07335ac6c` 镜像 `sha256:a4fad7388a22941d6c661de2f3c44fd01b59564d065944656460acd1db633a7c` 和切换前完整环境。
- 回退只替换 API/Web，保留 schema41、全部玩家与协作数据；不执行迁移 DOWN、旧库回灌、权限重放、数据卷删除或基础设施重建。

本记录之后的纯文档提交不代表再次构建或部署应用。
