# 九层妖塔免费联网版发布记录（2026-09-09）

> 九层妖塔已部署并完成公网验收。北京时间 2026-09-09 09:23:08.917 通过生产运行门禁，09:23:51.349 通过完整线上验收；应用版本为下列完整 SHA。本文文档归档不改变应用镜像版本。

## 版本与功能

- 分支：`feat/workstation-tower-defense`。
- GitHub 已同步、生产已运行：`82b84312d01cd9174441dc4a5ac45d9e56ea5a71`。
- 发布前生产及应用回滚基线：`2c82fddf8cf5a8bea6fec1420f081f149c4cb655`。
- 迁移：`0029 → 0030`，只新增六张妖塔表及相关索引、约束，不改写既有账号、钱包、其他游戏、聊天或开发附件。

新增站内原生 `/games/demon-tower`，不是嵌入附件 HTML。包含九层数据、五维成长、20 件武器、16 个技能、短回合探索、服务器存档与异步世界首领/通道协作；支持刷新续玩、便签遮罩、批量分点及免费滚动 24 小时配点重置。开局即可免费选流派，不要求凑人数或等到 16 级。

妖塔等级、经验、体力与材料独立；办公币沿用统一钱包。妖塔日常每账号每个北京时间自然日最多 200 币，有效首领伤害日榜第一名另得 100 币，次日 00:05 后结算；建设不折算日榜伤害。全程无充值、人民币价格、付费战力或第二通用货币。现有游戏和存档保留，旧 OfficeBattle 不重新开放。

## 已验证证据及边界

最终冻结源码的完整测试合计 **1,734 项通过**：后端 121 套件 / 905 项（152.125 秒），前端 121 文件 / 829 项（56.03 秒）；根目录测试进程 exit0，类型检查和社区构建通过。不得把不同预览轮次或额外模拟数量相加为正式测试总数。

开发期真实 PostgreSQL 覆盖过账号归属、同编号重放、版本竞争、末击与建设、停用/注销、跨午夜回滚、免费重置及旧模块回归。真实 Nginx / Firefox 覆盖自然成长与战斗、配装、刷新、响应丢失后的同编号重试、双标签页预算变化、390 像素布局及读失败恢复。390 像素视口不是物理手机验收；受控故障仿真和合成世界前置不冒充自然通关。详见[实施记录](DEMON_TOWER_IMPLEMENTATION_20260908.md)和[界面验收](DEMON_TOWER_UI_ACCEPTANCE_20260908.md)。

v3 隔离后台长测实际运行 **7 小时 52 分 36 秒**（2026-09-08 13:07:41—21:00:17 UTC），exit0：4,811 次请求、62 次真实 JWT 过期/续期，午夜及 00:05 边界检查通过。**它固定使用旧 v3 编译输入，且 worker 显式调用 `settleDue`，不能证明最终候选或 API 原生生产定时器已经通过自然跨午夜实测。** 不以后台时长冒充前台开发时长，也不替代最终镜像门禁。

另在仅有 `lo/127.0.0.1` 的独立 `network-none` 合成数据库栈中，以 `NODE_ENV=production` API 的原生 30 秒定时器验证昨日贡献补发：2026-09-09 01:22:33.994 UTC 完整通过，34 次观察、300 秒稳定无重复；结果为 1 奖项、1 奖励凭证、2 笔钱包流水、1 通知。两笔流水是正常初始 500 币加冠军 100 币，合计 600；124 张非奖励表保持不变。该测试无生产数据、不手动调用结算服务、不改时钟，但使用昨日合成前置，**不是自然午夜测试**。最初包装脚本因 Redis CLI 参数拼接在种数据前失败；保留证据后明确恢复并首次种入测试前置，复验通过，不计为产品故障。

预发布的全合成 HTTPS 回环栈已验证原冒烟脚本 46 次请求、6 次零奖励动作，随后精确清理两名临时普通账号并通过 132 表指纹核验。清理脚本的 `name[]` 元数据解析问题仅通过显式转换 `text[]` 修正，没有放宽复合外键、身份、默认资料或删除守卫。该结果不是生产 HTTPS 验收。

## 正式发布流水

| 阶段 | 当前已确认状态 |
| --- | --- |
| GitHub | 上述完整应用 SHA 已推送 |
| 新鲜生产备份 | `backup.sh` exit0；完整恢复成功，恢复库仍为 schema29 |
| 异机备份 | 清单内 8 个文件 SHA256 全部通过，校验标记已返回；不把 manifest 本身计为第 9 个内容检查 |
| 正式 API / Web 镜像 | 构建及资源检查通过，镜像 ID 见下 |
| 最终镜像七组真实 PG | **102 场景全部通过**：账号 9、资产 10、社交 18、Play 12、新闻快照 11、Rail 11、妖塔 31 |
| 私有备份克隆迁移 | `0030 up → down → up → noop` 通过；旧 126 表内容与结构、迁移记录及正确序列变化均核对 |
| 生产迁移和应用替换 | 仅新增 0030、仅替换 API / Web，部署 exit0；PostgreSQL、Redis、网关及数据卷未重建 |
| 公网 HTTPS / 页面资源 | 46 请求、6 次零奖励动作、2 名普通临时账号验收及精确清理通过；独立 22 次公开 GET 与 6 个静态资源检查通过 |

正式镜像 ID：API `sha256:151d73455b7816fa347eae64b21773b35197dbd4afbebcdd2d69a9330a3fe9cd`；Web `sha256:6de8ef3aec4dc7b9e31293d5f16ecc5d9a92dab6193840a242c1ff0361a52d94`。公网入口 HTML（`index.html`）的 SHA256 为 `81acc2b7d14ec64de7ca44cd24e94e512620661b4b162782c1cc67e553742ae3`，与候选产物一致。目录实际显示已启用、20 武器 / 16 技能 / 9 层；私有接口匿名返回 401，旧 OfficeBattle 返回 404，遮司仍使用同源 `SAMEORIGIN` 嵌入例外。

七组正式 PG 首轮因 Play 误复用含 132 表的非空合成库，被硬闸安全拒绝；原库改名保留，创建真正空库后重新运行全部七组通过，没有放宽空库或清理守卫。正式场景数不与开发预览、长测请求或 1,734 项回归测试相加。

新功能前后端统一使用 `FEATURE_COMMUNITY_DEMON_TOWER_ENABLED`，代码默认关闭，本次经验证的正式构建与 API 同时启用；其他开关与真实成员角色未变更。生产业务写入验收仅使用上述两名普通临时账号的 6 次零奖励动作；全部测试会话退出，相关测试账号与个人记录已精确清理，不制造首领伤害、建设贡献或奖项。首次入塔可以正常保留 9 行默认世界，不能将其描述为所有共享表逐字节未变。

## 回滚与隐私

优先回退到已保留的 `2c82fdd` API / Web，并关闭新功能。**生产禁止执行 `0030 down`、删六张新表、用旧备份覆盖上线后真实数据或回收已发办公币。** 新存档、贡献和财务记录保留，数据库、Redis、网关身份与数据卷不变。

旧版账号注销是软删除，不认识妖塔个人数据。启动旧 API 前必须临时设置 `FEATURE_ACCOUNT_DELETION_ENABLED=false`，同时暂停旧注销入口与处理队列，保留原申请、日期和租约；不得手动调用旧注销 worker，也不得把暂停写成注销完成。尽快以前向修复恢复新版清理。正常新版会清除个人妖塔存档、回执和贡献，取消历史奖项的身份关联，而不倒退共享世界或擅删财务历史。具体门禁见[部署说明](../deploy/COMMUNITY_DEPLOYMENT.md)。

## 时长与未验证范围

用户授权窗口为北京时间 2026-09-08 20:02:03 至 09-09 06:02:03。会话曾因额度中断，前台没有连续工作满该窗口，也未在原截止时完成发布；用户约 09-09 09:00 要求继续后恢复收尾。后台长测独立完成，不能抹去这次中断或提前宣称已经上线。

本轮不涉及真实付款或收费服务；未测试真实外部邮件/审核等第三方 Provider、物理手机及大规模高并发容量，不宣称具备识别所有多账号/自动化的能力。攻击性与破坏性测试仅使用隔离合成数据，不对生产真实用户执行。原生定时器已覆盖正常自动补发与幂等稳定性，尚未覆盖最终候选的自然跨午夜完整运行。
