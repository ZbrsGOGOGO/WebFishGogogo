# 2026-09-07 开发协作发布

## 结果

- 用户明确要求直接部署，并指定 4 个现有账号开通开发协作。
- 线上代码、API/Web 镜像标签：`5f0f25655f2e9df9605d3b155b891427db52e9ab`。
- 功能提交：`36e85d56e93cd66169bdd6347cb304c23f2f8dc8`；静态资源权限修复：`5f0f256`。
- 服务器目录 `/opt/webfish-review`；项目保持 `webfish-community`，未重建 PostgreSQL、Redis、Gateway，未删除生产数据卷。
- 数据库最新迁移 `1700000000026`，4 张协作表及附件实际字节长度 CHECK 已确认，生产开关为 true。
- 4 个真实账号均开通 contributor 权限，全站角色保持 user。授权由离线运维路径写入 system 审计与站内通知，不冒充某个站长操作；没有修改密码。站长身份待用户进一步确认。
- 当前没有配置 AI 接口、自动审核任务或无人值守监控。
- GitHub 分支仍停在 `c362c2d`。HTTPS 推送缺少凭据；SSH 22/443 连接未成功。服务器通过已验证的 Git bundle 收到同一提交，部署完成不等于 GitHub 已同步。

## 验证

- 后端全量：96 套件、493 项通过；前端全量：105 文件、506 项通过。
- 前后端生产镜像构建、静态预检及 Nginx 配置检查通过。
- 用已存在的脱敏 0007 快照完成真实 PostgreSQL 16 的全量 up/down/up、0026/0025 独立回退、邮箱冲突中止及锁超时恢复演练。
- 隔离新库完成 10 组协作业务检查：多连接、8 并发相同幂等键、版本冲突、私有文件字节校验、核心成员隔离、审核/导出、通知/审计、授权与通知并发无死锁、撤权及开关关闭。
- 最终公网烟测 55 项通过，覆盖新私有 API 未登录拒绝、游戏 iframe 和旧 API 封闭边界；确认 `/development` 引用新构建的协作页面，而非只命中旧 SPA 回退。
- 额外创建临时合成账号，通过真实公网 HTTPS 验证账号登录、Secure/HttpOnly/SameSite/Path 刷新 Cookie、普通账号拒绝、核心成员提案、超过 1 MiB multipart 上传、下载字节散列一致、撤权、Origin 拒绝、刷新及注销。测试账号、会话、提案与附件已按精确身份清理，不使用真实用户密码。
- 站长审核能力已通过隔离数据库与页面测试；生产尚无经用户确认的站长账号，站长本人界面验收待授权后完成。本次不宣称已完成大规模并发容量验收。

## 发布中发现并修复

备份/发布 shell 使用 `umask 077`，新检出的游戏静态文件成为 600；Vite 保留复制文件权限，导致 Nginx 非 root worker 对《遮司》返回 403。先恢复线上 3 个受影响公开文件的读取权限，再在 community-web 镜像中固定公开目录 755、公开文件 644，新增回归并重建替换最终镜像。已用 nginx 用户验证读取，游戏与整站烟测恢复通过；私有附件仍在数据库，不在该静态目录。

## 备份、证据和回滚

- 服务器限权目录：`/opt/webfish-backups/release-36e85d5-20260907.3hD0xo`。保留旧环境、旧镜像标识、加密数据库备份/校验、迁移演练、构建、发布和验收日志。不要把密钥或环境内容提交到仓库。
- 加密备份已复制到站点所有者本机限权目录，并核对校验和；已在无网络临时 PG16 中完整恢复，确认回到备份时的 0025。测试数据库/网络仅保存合成或隔离恢复数据，验收后已清理。
- 回滚使用该目录中的 `rollback.sh` 恢复 `c362c2d` 的 community 应用和原环境，保留数据库 0026；脚本已准备但本次未执行完整应用回滚。不要运行 `0026 down` 或删除协作表/数据卷。
