# 2026-09-07 两回合塔防难度发布

## 当前状态

已完成代码推送、备份恢复验证、生产部署、线上验收和本轮已确认难度反馈归档。

- 授权范围：[V3 约定](./WORKSTATION_TOWER_DEFENSE_V3.md)。本轮不是长期塔防规划的全部实现。
- 本地/GitHub 基线：`8e488f324503461cd3a5e9847573023a7c37480b`，当前确认分支 `feat/workstation-tower-defense`，开始时工作区干净。
- 生产与回滚基线：`ba210647a5b550e62a7d4c5da9619a77bc8cc105`，开始时 API/Web 健康，数据库迁移 `1700000000026`。之前的发布后文档提交不影响应用代码一致性。
- 本轮功能提交：`cb471d08d021c3219bda643549b33227379124ec`，已推送上述 GitHub 分支。生产代码和 API/Web 镜像均为该 SHA 且健康；发布后文档补记不另行重建应用镜像。

## 本轮变更

- 两回合：低压经营 → 混合稽查与临时加班小 Boss；数值和刷怪节奏配置化，重新平衡真实购买、合成和布阵路径。
- 可见的定向零件保底；仍消耗局内金币，没有账号资产或真实付费。
- 两回合预警、敌人类型和首领展示、失败建议及手机适配。首页介绍同步为五塔、两回合，消除历史三塔/三波说明。
- 仅保存独立 V3 本机最高分，V1/V2 记录原样保留。

## 发布与数据保护

- 不新增数据库迁移，不更改账户权限、既有服务开关、Compose 或数据卷。只发布 API/Web。
- 本次独立加密备份及恢复验证目录：`/opt/webfish-backups/tower-rounds-20260907.g9mCUT/`；执行前核验当前基线和回滚镜像，保留异机副本。
- 发布脚本绑定本次目标与前版 SHA，部署前验证环境镜像标签，部署后核对实际镜像；除 `IMAGE_TAG` 外环境配置必须完全一致。异机验证标记绑定本次密文校验值。
- 原塔防总规划包含离线、职业、付费、多人等未授权实施阶段，不将这些内容随难度版标记为全部完成。反馈归档以本次站长确认的实际范围与线上验收为依据，并注明剩余范围。

## 最终验收结果

- 全仓类型检查通过；后端 97 个套件/509 项、前端 107 个文件/577 项全部通过，共 1086 项。社区模式全量生产构建通过。
- Firefox 最终构建在真实 390 CSS 像素视口完成开局购买、合成、部署、间歇定向补阵及完整第二回合。有效战斗约 95.543 秒，核心 10，得分 4255；看到真实小 Boss 血条，无运行错误，页面滚动宽度 378，无横向溢出。守卫未移动、未放技能，未注入金币或修改引擎状态。
- V1/V2 测试记录 7777/8888 原样保留，V3 只记录本局 4255，没有混分。最终构建也在 1440 像素桌面核验布局与结算；前一个候选完成约 95.515 秒桌面对局和键盘暂停冻结收入检查。两个浏览器验收都仅模拟登录/协作权限，游戏没有请求账号资产 API，不冒充生产账号对局。
- 公网脚本首次在注册限流探针遇到一次 TCP 443 连接超时，未修改脚本重跑后通过全部检查，共 54 次 HTTP 请求；重跑时已有注册限流，因此探针请求数与历史 55 次不同。另用精确创建的临时普通账号通过真实 HTTPS 登录、Cookie 属性、Origin 拒绝、刷新轮换、开发协作权限拒绝和退出检查；该测试账号及关联测试数据已清理，只读复验残留为零。
- 实际线上入口为 `/assets/index-BwRNqBuI.js`，塔防分包为 `workstation-tower-defense-DviVZY_O.js`，与本地最终游戏分包一致。真实响应包含两回合、定向订货、首领、预警和 V3 标识，内容类型正确；数据库就绪检查正常。

### 反馈与权限

- 本次站长确认的难度反馈从 `submitted` 版本 2 归档为 `done` 版本 3，新增一条系统审计和一条作者站内通知，记录完整本轮发布 SHA。归档说明明确只覆盖本次确认范围，不代表正文复用的长期规划已实现。
- 原塔防长期总提案仍为 `submitted` 版本 2；后补的买后空位规则尚未收到单独确认，独立反馈保持 `submitted` 版本 1，本版继续自动补货。两条此前农场反馈仍为 `done` 版本 2/4，原附件保留。
- 最终只读核验：账户角色与四名开发成员权限未变，没有提升或冒充管理员；迁移仍为 `1700000000026`。

### 备份与回滚证据

- 本次加密数据库备份校验成功，并完整恢复到 `--network none` 的一次性 PostgreSQL 16 实例验证；临时恢复容器已移除。密文、密钥和发布前配置的异机副本保存在操作者受限状态目录 `webfish-backups/tower-rounds-20260907.g9mCUT/`，目录 700、敏感文件 600，均不进入 Git。
- 生产只更新 API/Web；PostgreSQL、Redis、Gateway 的容器 ID 与发布前一致。回滚脚本保存在本次服务器发布目录，使用完整本轮 SHA 为确认参数，恢复前版 `ba21064` 的 API/Web 与配置；已准备但未执行，不回退数据库或撤销真实反馈。
- 测试、构建、浏览器证据、备份恢复、公网、真实 Cookie、归档和最终状态核验日志保存在受限发布目录。私有反馈原文、附件、密钥和环境文件不提交公共仓库。

### 已完成的难度与经济验证

- 第一回合 6 只、原始总生命 72；第二回合 24 只、原始总生命 288。4 倍指数量/生命配置，不声称实际通关难度可用 4 倍量化。
- 成团群怪非范围单次伤害上限 2、突破冲击 3；打印机范围攻击穿透 2 点护甲。真实机制展示在敌情/塔说明中；配置的默认出怪间隔经测试确实参与调度。
- 开局绿植和保底二阶单塔约 78.96 秒失守；只把单塔升到三阶，约 91.84 秒过关但核心仅剩 4。控制阵容（二阶订书机第 5 位、咖啡机第 3 位、转椅第 7 位）在 5 个种子下均约 95.48 秒满血清完 30 个敌人。
- 通关测试使用实际初始金币、绿植产币、首回合一次性 50G 结算、真实购买和部署；不注入资金或攻击力，不操作守卫/技能。间歇 160G 买咖啡与转椅后剩 1G，可停止操作并自动防守。
- 引擎 27 项、页面 25 项定向回归通过；商店切换、刷新、合成、满包、出售及重复操作经独立只读模拟审查；第一回合结算与暂停不能重复增发金币。
