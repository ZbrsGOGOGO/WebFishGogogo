# 文字战线 V4、持久房间与妖塔魂珠发布记录（2026-09-19）

状态：**应用代码已推送 GitHub `main`，API / Web 已上线并完成分层验收，三条协作反馈已按实际范围回填**。API 于北京时间 2026-09-18 12:25:43 切换，最终 Web 路由修复于 2026-09-19 16:16:09 切换。后续纯文档提交不重建应用。

## 实际运行版本

| 对象 | 完整提交 / 镜像 |
| --- | --- |
| API 应用 | `ef93db12d3a900e3e3506e5d595a6b9f5d7e813e` |
| API 镜像 | `sha256:90ce0e1a1907822bf5aae8d35e581811e5599d163e8b9eedb863cd7a86cca958` |
| Web 应用 | `3e32257d4bc46329482c7ba3d4cacb5dd996abb2` |
| Web 镜像 | `sha256:b9dee18c35041ccefa15c47354dea0844ac3c58deacfb32b5d9cfaac836457e2` |
| 应用功能提交 | `ef93db12d3a900e3e3506e5d595a6b9f5d7e813e` |
| Web 路由补丁 | `fbc8410`、`3e32257` |

最终 Web 仅比 API 功能提交多两个 Nginx 路由修复，后端源码树没有差异。生产源码固定到 `3e32257`，运行 API 仍使用已验收的 `ef93db1` 镜像；不能把这个混合标签说成 API 漏部署。

数据库已执行一条增量迁移，当前为 **schema40 / 41 条迁移 / 150 张 public 表**。PostgreSQL、Redis、Gateway 的容器完整身份未变；没有重建基础设施、删除数据卷、回灌旧库或运行 DOWN。

## 交付内容

### 文字战线 V4

- `/tower-defense/word-front` 默认进入 V4；v1、v2、v3 入口、成绩和历史规则保留。
- 三张 10×8 地图包含路线、障碍、草地、增益格和可展开全图；十二名角色、有限字池、消耗式抽取及赵云字对保底由共享确定性引擎执行。
- 剧情 20 波、无尽 40 波，八类敌人、阶段首领、五件专属神兵、局内金币与商人均纳入服务端可重放状态。
- V4 使用独立游戏键、固定种子和动作回放校验后记榜，不接办公币、每日冠军奖励、账号装备或真实付费。
- 免费双人房使用同一 V4 规则，可选房主密码；服务端权威推进、动作幂等、离开结算及数据库快照支持 API 重启后恢复。房间仍为 1V1，不伪装成大规模联机。
- 站长专用地图草稿页受服务端管理员权限保护；草稿校验和预览不直接替换正式内置地图，也不把前端隐藏当权限控制。

原型来源固定为公开仓库 `https://gitee.com/wang-luqi/touch-touch---mini-game.git` 的提交 `debbda...`，按 MIT 许可记录。生产实现为本站 TypeScript 确定性规则与自有界面，没有提交原型凭据、私有附件或下载缓存。逐项表现对齐和真人长期平衡仍需继续观察，未把整份新提案标成完成。

### 九层妖塔双魂珠

- 每把主武器维护独立的两个魂珠槽，不在换武器时串槽或覆盖其他武器配置。
- 五类魂珠、七级成长、合成来源和战斗效果进入现有角色存档与结算；周免费领取与绑定魂力不接办公币、支付或期权权益。
- 未实现的关系技能、神技升阶、周期活动、轮换商店、黄金卷轴及独立签到继续保留为待办，不借魂珠发布顺带宣称完成。

### 生产路由补丁

首次公网核验发现管理员地图接口被 Nginx 静态白名单挡成 404。补丁只开放精确的 `/api/community/tower-defense/word-front/maps` 及受限子路径，仍由 API 返回未登录 401、非管理员 403；相邻未列入路径继续 404。

`fbc8410` 的候选 Nginx 镜像因未引用的量词正则无法启动，只在隔离候选阶段失败，**未部署生产**。`3e32257` 引用该表达式后通过真实 Nginx 启动与代理测试，再做 Web-only 切换；API 和游戏房间没有因这次 Web 修复重启。

## 验证结果

| 层次 | 结果与边界 |
| --- | --- |
| 完整后端 | 164 套 / 1637 项通过；另 6 套 / 85 项按既有条件跳过，不计为通过 |
| 完整前端 | 221 文件 / 1939 项通过 |
| 类型与构建 | 全工作区类型检查、shared 构建、API/Web 冻结依赖镜像构建通过 |
| 恢复与迁移 | 旧备份恢复为 148 表/40 迁移；隔离 UP/DOWN/UP 后为 150 表/41 迁移，3 张 10×8 地图有效，0 未验证约束 |
| 候选 API/Web | 健康、V4 榜、两项认证门禁、Web 路由和许可文件通过 |
| 最终路由 | 独立真实 Nginx 代理验证通过；管理员地图接口匿名 401，相邻未知路径 404 |
| 公网发布 | 67 项 HTTP 烟测通过；四个关键功能资产与运行 Web 镜像哈希一致，三个健康入口通过 |
| 生产认证 Cookie | 明确跳过：没有获准的生产合成账号凭据；不把匿名 401 或隔离测试冒充登录验收 |

完整恢复比较忽略的只有 `pg_dump` 随机 restrict 标记及 UP/DOWN/UP 引起的迁移序列自然前进；没有忽略玩家业务行。旧业务数据规范化摘要在迁移前后均为 `62700d099f1bd31766c115d8c522d03e38d7f781dc92fce062e7946ef84af567`。

## 备份与回退

部署前备份位于生产私有目录 `/opt/webfish-backups/collab-20260918.s8T6i7`，加密归档 SHA-256 为 `22be058e6553f8a32b6366cd4087c40725922d5864f9a2faa220518df8854337`；九个离站文件和 Git bundle 已逐项校验。密钥、口令、环境文件、数据库内容和真实反馈附件不进入 Git。

直接回退使用已保留的上一稳定应用 `f346f0c413d653203ae7f4c8d9ed2eadb8276e76`：API 镜像 `sha256:6f4d25ca2204c47d9581586aa71e7162e21346fe962538b7020c2d18d2af57ee`，Web 镜像 `sha256:04ea075460d0116dbc48e135e525e2afef9c01e0905f485a44a1a32bfa745eb8`。真实恢复副本已验证旧 API 可映射 schema40 并保留未知妖塔 JSON 扩展；回退保留 41 条迁移、V4 成绩/地图草稿/房间快照、魂珠存档及所有玩家数据，不运行 DOWN、不回灌备份、不重建 PostgreSQL/Redis/Gateway。旧应用期间 V4 与魂珠不可操作，恢复新应用后再继续；不得用更早且缺少猜画隐私清理的裸镜像替代该版本。

## 协作反馈回填

2026-09-19 16:21:13（北京时间）重新读取全部反馈并确认没有比本轮基线更新的评论，再通过运维专用进度入口写入分项审计和作者通知：

- 新「赵云救阿斗全新重构」：由 accepted v3 更新为 **in_progress v4，9/10**；只保留原型细节对齐和真人长期平衡为进行中。
- 「九层妖塔 v0.11」：由 v5 的 6/11 更新为 **v6，7/11**；魂珠完成，其他未完成项原样保留。
- 旧「塔防游戏大更改」：由 v5 的 4/9 更新为 **v6，7/9**；完整地图、持久房间和 V4 默认替换计为完成，真人平衡进行中，局外商店仍待开发。
- 全站快照为 **34 条：31 已完成、3 进行中、0 待审阅**。三份大型提案均未被虚假关闭。

暂停的 Palm AI、额外游戏接入、办公币/付费规则、成员权限与基础设施配置不在本批范围；根工作区中属于用户的未提交 Palm 文件也未进入任何提交、镜像或部署。
