# 2026-09-07 农场反馈修复发布

## 结果

- 站点所有者明确要求今后每次修复验证通过后默认提交、推送并部署；已记录于根目录 `AGENTS.md`。这不是定时监控任务，开发反馈仍由所有者手动提醒后处理。
- 农场修复提交与线上 API/Web 镜像：`d8e814e0e681af43a8e584846c6bdba7d12fbce6`。
- 已原生快进推送到 GitHub `ZbrsGOGOGO/WebFishGogogo` 的 `feat/workstation-tower-defense` 分支，之前未同步的 `36e85d5`、`5f0f256`、`fa59a52` 一并保留原始提交历史并推送。不强推、不改 `main`。
- 本机 HTTPS 推送缺少凭据，但既有 SSH 22 授权已验证通过；为 origin 保存 SSH push URL，fetch URL 保持 HTTPS，没有新建或导出密钥。
- 生产 `/opt/webfish-review` 检出同一个 `d8e814e`；API/Web 均 healthy。仅修改 `IMAGE_TAG`，既有功能开关、隐私配置、账号权限不变。
- 数据库仍为 `1700000000026`，没有执行迁移、回退迁移、成员授权或真实用户农场数据修补。
- PostgreSQL、Redis、Gateway 的容器 ID 与发布前完全一致；没有重建数据服务或删除生产数据卷。

## 验证

- 修复前后完整依据见 [农场反馈修复记录](./FARM_FEEDBACK_2026-09-07.md)。本地后端 96 套件/495 项、前端 107 文件/544 项全部通过；全仓类型检查、构建、差异格式检查通过。
- 本地 Firefox 验证准确种植预算、手动换低价作物再浇水，以及 390 像素手机视口无横向溢出。
- 生产镜像构建、静态预检、Nginx 配置与 nginx 用户读取游戏公开资源检查通过。离线 Nginx 配置检查显式映射 `api` 主机名，不依赖生产网络；发布门禁检查明确的成功标记，不用“日志非空”代替成功。
- 公网 community smoke 的 55 项 HTTP 检查通过，包括健康检查、页面、小游戏同源 iframe、开发协作鉴权、未开放旧 API 拒绝及安全响应头。
- 实际 `/farm` 引用的 `/assets/index-C0d0eLd6.js` 包含新版种植预算、余额不足与刷新文案，不是只命中旧页面或 SPA 回退。
- 独立合成账号通过真实公网 HTTPS 验证：登录与安全 Cookie、普通用户无开发提案权限、首轮免费、老玩家空闲时 `firstCycle=false`、440/237 种子不足且不扣款、手动切换番茄后扣 100 正常种植、同键重放只扣一次、成熟收获收益到账但不足续种时保留成果、重复收获不重复奖励。
- 合成账号及其会话、农场周期、余额、流水均已按精确身份清理；随后只读确认此轮测试账号残留为 0。真实玩家未被代浇水、代收获或扣币，线上提案状态未冒充站长修改。

## 备份与回滚

- 新鲜的 **0026** 数据库备份、当前环境副本、镜像 ID、构建与验收日志、独立回滚脚本保存在服务器限权目录 `/opt/webfish-backups/farm-d8e814e-20260907.4BOKMm/`。
- PostgreSQL dump 使用独立随机密钥加密，散列与 `pg_restore --list` 通过；已在无网络的一次性 PostgreSQL 16 容器完整恢复，核实最新迁移 0026 与关键表。临时恢复容器已清理。
- 加密备份与密钥复制到本机 `/home/zbrs/.local/state/webfish-backups/farm-d8e814e-20260907.4BOKMm/`，目录 700、文件 600，异机副本校验通过。不要把密钥、环境副本、备份内容提交到仓库。
- 本轮独立 `rollback.sh` 固定恢复 **5f0f256** 的 API/Web 与发布前环境，保留 0026 数据库和全部新反馈。脚本已校验并保留，未因验收成功而执行回滚。
- 不得复用上一轮将应用回退到 `c362`、数据库备份尚为 0025 的旧发布脚本。本次未清理可回滚的旧镜像。

本发布记录为部署后的文档补充；记录本身的提交不改变 `d8e814e` 业务镜像，也不需要为文档单独重启网站。生产代码版本以上述镜像 SHA 为准。
