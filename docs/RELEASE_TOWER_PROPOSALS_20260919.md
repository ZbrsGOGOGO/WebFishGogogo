# 妖塔 v0.11 与旧塔防提案收口发布记录（2026-09-19）

状态：**功能代码已推送 GitHub `main`，API / Web 已部署并完成生产验收，两条协作提案已逐项回填并归档完成**。应用功能提交为 `ac60b0a302e55addd43939b6cd98c0398c67ed9c`；实际运行提交为包含部署预检说明的 `590db0b3ec08f1fb2bdec5e48e0f8371e6ac3120`。后续纯文档提交不重建应用。

## 实际运行版本

| 对象 | 完整提交 / 镜像 |
| --- | --- |
| API / Web 应用 | `590db0b3ec08f1fb2bdec5e48e0f8371e6ac3120` |
| API 镜像 | `sha256:ba44dd5fda93418c85381e44f40f615a00c0d872a8a39649e7f4ea583ef3b3bc` |
| Web 镜像 | `sha256:aca7ef7c812f58571f35ccb0b34e002c5cb39867512e56bc0cbaa2077936aba8` |
| 功能提交 | `ac60b0a302e55addd43939b6cd98c0398c67ed9c` |

API 容器为 `93dd89080025a195f9a1d5238eaeec61a1a7b1311a8f8e5a31855f99fff2651b`，Web 为 `a27ec56d779199fed111833452212bb46e572649e58f5e6e1a0fdbbb2085f76e`，均为 healthy。PostgreSQL、Redis、Gateway 的容器完整身份未变；只顺序替换 API / Web，没有重建基础设施、删除数据卷或回灌旧库。

数据库执行 `AddWordFrontProgress1700000000041` 后为 **schema41 / 42 条迁移 / 151 张 public 表**。新 `word_front_progress` 表上线时为空；旧 150 张表的迁移演练前后逐表内容摘要一致。

## 交付范围

### 九层妖塔 v0.11

- 保留现有升级、魂珠、战报、任务和武器养成；新增独立每日签到，不重复借用全站签到。
- 上线每日常驻、每周轮换的绑定魂力商店，以及经验、体力补给；不接办公币、支付或期权权益。
- 上线黄金卷轴、祝福保底和节庆活动升星，保留失败不降星的免费边界。
- 补齐 `s21`–`s26` 六项技能的战斗效果、装备与升级预览；与既有 `s17`–`s20` 及神武终式并存。

### 文字战线 / 旧塔防提案

- V4 默认入口、三张 10×8 地图、十二角色、有限字池、装备、局内商人、服务端回放榜及可跨 API 重启恢复的双人房保持不变。
- 新增独立账号功勋、每日获取上限和免费局外商店；不读写本站办公币。
- 购买的装饰进入账号进度并在 V4 / 地图页展示，不影响旧 v1–v3 存档和榜单。
- 管理员可查看近 30 天真实聚合平衡数据；普通玩家接口仍受登录/管理员权限保护。

## 验证结果

| 层次 | 结果与边界 |
| --- | --- |
| 完整后端 | 167 套 / 1651 项通过；另 6 套 / 85 项按既有条件跳过，不计为通过 |
| 完整前端 | 223 文件 / 1950 项通过 |
| 类型与构建 | 全工作区类型检查、前端生产构建及冻结依赖 API / Web 镜像构建通过 |
| 迁移演练 | 生产新鲜副本完成 UP / DOWN / UP：41/150 → 42/151 → 41/150 → 42/151；旧 150 表内容摘要始终为 `fc8a90100b80e6854c2ec38c0310dd2bbc336175ddacb4849908f70fe0846c2b` |
| 候选环境 | 隔离 PostgreSQL、API、Web 真实启动；健康、权限门禁和旧 API 对未知妖塔 JSON 的兼容探针通过 |
| 生产本机 | 三个健康入口 200；妖塔、文字战线进度及管理平衡接口匿名 401；数据库 42 条迁移 / 151 表 |
| 公网 | 三个健康入口及妖塔、V4、地图页面均 200；新受保护接口匿名 401 |
| 生产认证 Cookie | 明确跳过：没有获准的生产合成账号凭据；不把匿名 401 冒充完整登录验收 |

首次 SSH 尝试在握手阶段中断；同一密钥和别名稍后恢复，端口、主机指纹与授权键均未改变。服务端日志只留下了一次 `kex_exchange_identification` 连接重置，没有 `MaxStartups`、认证失败或封禁记录，因此无法把根因精确归到 SSH 配置；现有证据更符合短时网络或会话连接重置。此次没有修改 `sshd` 或放宽安全策略。另一次验收命令漏写 `sudo` 只导致 Docker socket 拒绝，未影响服务或部署。

## 备份与回退

部署前新鲜私有备份位于 `/opt/webfish-backups/tower-proposals-20260919.yrjYRX`，数据库 dump SHA-256 为 `e03edacbff82ea240567242cbfb70eedd02397588c10c8c48486951e0209428e`；归档、配置、容器身份、Redis 数据和 Git bundle 均已校验。离站 AES-256 加密归档 SHA-256 为 `0fb23fae579dc1b58c26f579164e5ecbea75711ef88ef6618b8356f7040b13d5`，已完成解密与目录验证；密钥、环境、数据库及玩家附件不进入 Git。

直接应用回退使用切换前 API `ef93db12d3a900e3e3506e5d595a6b9f5d7e813e` / `sha256:90ce0e1a1907822bf5aae8d35e581811e5599d163e8b9eedb863cd7a86cca958` 与 Web `cbc17e54d2208aec5d0043aad94ca4a04d4b25c5` / `sha256:e620e862475e9a1d744f901bc3c170a8fc0170b4578ad9db06929a67b53a0ee7`。旧 API 已在真实引擎验证会保留未知妖塔扩展；回退保留 schema41、42 条迁移、文字战线进度和全部玩家数据，不运行 DOWN、不回灌、不重建 PostgreSQL / Redis / Gateway。旧应用期间新功能不可操作，恢复新应用后继续。

## 协作反馈

部署后重新读取全部反馈：当时仅两条进行中、没有新评论或新提案。先用运维专用进度入口记录真实完成分项，再用完成入口归档，审计与作者通知均绑定实际运行提交 `590db0b`。

- 「九层妖塔 养成与数值对齐设计底稿（v0.11 规划）」：`11/11`，最终 **done v8**。
- 「塔防游戏大更改--赵云救阿斗」：`9/9`，最终 **done v8**。
- 最终全站快照：**34 条已完成、0 进行中、0 待审阅**。

暂停的 Palm AI、额外游戏接入、真实付费、办公币规则、权限扩大和基础设施配置不在本批范围；根工作区中属于用户的未提交 Palm 文件未进入提交、镜像或部署。
