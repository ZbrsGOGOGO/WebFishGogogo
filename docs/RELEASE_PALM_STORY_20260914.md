# 掌心故事本地体验发布记录（2026-09-14）

## 范围与合规边界

- GitHub `main` 功能提交及生产 Web 源码提交为 `9994fe9c2ee0cbfe790d2e00c53e9b20df95da7f`。北京时间约 18:14 完成 Web-only 切换。运行 API 仍是 `d641ae0b0d6341a6ac3ed69db58e6ddd521362c1` 原镜像，不能把文档或 Web 提交号当作 API 实际版本。
- 工具页新增独立「掌心故事」卡片及 `/tools/palm-story` 深链；原 11 款工具目录和计数不变。用户可选左右手照片作本地参考，手动选择纹路形状，再获得标注来源的主题、表达/工作/人际练习及五步自我探索。出生日期/时段只作季节与配色意象，单独填写不能生成报告。
- 这是原创、免费的娱乐体验，不复制参考网站代码、图片、品牌或文案，不调用对方 API。**照片不上传、不自动识别，也不做 AI 掌纹、精确四柱或结果预测。** JPG/PNG/WebP 单张限 8 MB，空文件、无法读取和坏图均提示；关闭/清空页面释放内存中的预览和输入。无后端接口、存储、账号、办公币、付费或排行榜变更。旧 `full` 本机模式不提供此入口；生产 `community` 和公开/审核路由可用。

## 验证与数据保护

- 最终前端全量 **199 文件 / 1693 测试通过**，定向 10 项、全仓类型检查和前端构建通过；独立 QA 复核图片错误/异步读取清理、CSP、焦点、隐私声明及 320px 响应式。浏览器在本地开发版验证生成与 390/320px 无横向溢出，生产构建镜像导出的静态文件哈希一致，并在浏览器中复验深链与生成。没有以真实玩家账号测试生产其他功能。
- 生产切换前新鲜全环境/数据库备份在限权目录保留，加密档案已异机校验。隔离 PostgreSQL 完整恢复为 **40 条迁移 / 148 张表 / 245 条 Arcade 对局 / 8 条最佳成绩 / 0 未验证约束**；克隆容器及卷按确切名称清理，原加密备份保留。此次后端、共享包和迁移源码相对运行基线完全不变，生产没有运行 UP/DOWN。
- 最终 Web 镜像 `sha256:f028500979e592e0b289569dbb7a3925c703821900762d418dbd4f524eff2255`，API 继续为 `sha256:e38805ec45b691816b270e7cdb4b3359d9dc5cd4802b56b94b5cb16d02bfe20f`。候选 Web 在独立测试容器验证 `/tools/palm-story` 与懒加载资源 HTTP 200；完整生产构建静态文件在本地浏览器复验报告交互后，测试容器/网络清理。

## 生产验收与回退

- 仅更新 `.env.community` 的 `IMAGE_TAG`，旧 API 镜像以新标签指向相同不可变 ID，API 容器本身未重启；只使用 `--no-deps --no-build --pull never --force-recreate --wait web` 替换 Web。Web 健康，公网 **65 项 HTTP 烟测通过**；`/tools` 显示新入口及旧 11 款工具，公网访客通过新深链手动标记成功生成完整报告。API 就绪，生产仍 40 条迁移；API、PostgreSQL、Redis、Gateway 容器 ID 保持切换前值。
- 如需撤回本次前端体验，先核对当前配置和旧镜像，使用保留的 `d641ae0` Web 镜像 `sha256:81abd64f421235ae521f55b5b7d2e5c40213e90e8e80af36e40829a2fbfd19d0` 及切换前完整环境，**只重建 Web**，验收公网入口。API、数据库、Redis、网关及数据卷保持原状；不执行迁移 DOWN、旧库覆盖或删除玩家数据。底座 v3 API 若另需回退，必须按 [v3 发布记录](RELEASE_WORD_FRONT_V3_20260914.md) 使用 s17–s20 兼容桥，不把这次 Web-only 回退当作 API 回滚。
