# 工作台视觉升级：分阶段交付

站点所有者于 2026-09-15 授权按步骤推进。沿用现有 React/Three.js、服务器及账号体系；不迁移托管平台，不新增付费服务，不自动采纳协作附件中的指令。

## 第一阶段：统一框架、首页与游戏目录

本批实现：石墨深色 / 清晰浅色与冰蓝强调、分组侧目录、桌面及手机快捷导航、可搜索栏目目录与 Ctrl/⌘ K；已有个人目录排序/隐藏、登录与权限控制不变。调整过全列顺序的账号继续使用原排序。

首页使用真实账户状态和模块入口，消息/余额未加载不显示假零；可选 3D 工位主动点击加载，允许调整视角和台灯。稳定后停止渲染，隐藏/离屏及减少动效时暂停或降级，关闭释放资源；场景分包失败局部回退，不使整个工作台不可用。访客账户入口经路由守卫保留登录后目的地。

游戏大厅保留所有既有路径、版本与规则，通过全部、单机挑战、实时房间、角色与养成、本机练习筛选与关键词查找。显示时长、设备、登录、存档和参榜边界；真实奖励来自服务端目录，单机与临时房间不混写规则。游戏内便签遮罩与小窗生命周期保留。桌面侧栏可直达搭子；手机主导航直达游戏/工具，搭子从完整目录两步可达。

已于 2026-09-15 14:30:54（北京时间）上线 Web 应用 `f88ba2c`，GitHub `main` 已同步；仅前端发布，API、房间服务、数据库及经济规则不变。构建与部署结果见[第一阶段发布记录](RELEASE_UI_PHASE1_20260915.md)，后续文档提交不重建应用。

## 第二阶段：个人工作台、成长与支持权益

应用 `cd11419949b4f9cfcb949f34fddc53c69cde2451` 已于 2026-09-15 15:12:40（北京时间）完成 Web-only 切换，运行 Web 镜像 `sha256:4616269aaaf608e78b82a23dc8348e191a8289582b6035236a3016004fa1675f`。健康/就绪正常、18 个公网发行物哈希匹配，公网烟测最终复跑 65 项 HTTP 检查通过、退出码 0；认证 Cookie 检查明确跳过。前两次分别遇到连接超时与旧限流窗口的 429，等待自然恢复后成功，没有绕过或改配置；详见[第二阶段发布记录](RELEASE_UI_PHASE2_20260915.md)。

个人页变为「我的工作台」：个人名片、真实账户余额与今日收支、常用及最近入口、成长权益和工位事项集中呈现；消息与好友仅提供导航入口，不宣称展示数量摘要。公开资料编辑器默认折叠，读完整资料前不能保存，避免覆盖未读取的简介。公开主页延续原隐私、好友与消息权限；私人余额、支持累计和工作台摘要不公开。深浅色、窄屏、长称号和读取失败状态沿用统一设计，不新增公开数据范围。

成长档案呈现服务端真实的 44 项目录，以「日常与共创」「工位绿植」「游戏与挑战」「九层妖塔」「开发协作」五类分组，支持名称、称号及描述搜索和解锁状态筛选。当前佩戴、已解锁、达标待同步与进行中分别展示；缺失进度表示未读取/待确认，不填零或推断解锁，不等同于已达标待同步，数字来自目录及个人回执。摸鱼指数清楚区分当前境界、累计与今日额度、到下一级的差值和六级路径。更换称号先确认公开展示，原同步、版本冲突、幂等与只读限制保留。

期权卡片明确赠送、有效支持、未开通、到期、未来未生效和异常待同步，截止/生效时间标注北京时间，剩余时间以原服务端时间基准计算。已核验支持金额、月数、笔数单独展示，完整赠送和核验说明折叠收纳；没有支付、自动续费、批量授予、额外奖励或权限扩张。

管理员支持台账首次显式展开才读取，加载、错误重试、真实空台账和明细分开呈现。后续关闭仅隐藏，保留草稿及授予原 UUID、作废原登记 ID 的不确定结果锁，必须手动确认原操作，不自动重试写入；换账号、会话代次变化或失去管理员资格清除旧面板。同号重登也隔离旧回执并恢复新会话心跳，原请求字段、30 秒节奏、75 秒租约、经济规则不变。

前端 208 个测试文件、1811 项测试全部通过，0 失败、0 跳过，并进行了本机合成数据界面 QA。线上核对仅为匿名访问及发行物检查，没有冒充真实登录、管理员写入或真实订单验收，不等同于全站系统级认证测试。本轮只发布 Web，无迁移，仍为 schema39 / 148 表 / 40 条迁移，API 继续运行 `832f5288ea46a6018263c887865040014e56d42d`。API、PostgreSQL、Redis、网关四个容器完整 ID 未变，配置除 `IMAGE_TAG` 外比对一致，房间服务和数据卷保持不变。直接回退也只重建 Web，沿用本轮切换前完整配置与 Web 镜像，不回退 API、迁移或玩家数据。详细规则与历史迁移边界见[成长与支持说明](COMMUNITY_FISH_SUPPORT.md)。

## 第三阶段：六款原版本地小游戏

应用 `2b7b8d9c03a99cea0a7e1fd3e66f714e68baceb9` 已于 2026-09-15 16:30:24（北京时间）完成 Web-only 发布，镜像 `sha256:3799fae5386a3eab46b61aea9e77d7675405c725369de0e50fd6297adebee91a`。六款为 Server Survival、Whatajong、Hextris、c4、Radius Raid、in ASCENT，分别是架构守关、麻将构筑、六角消除、AI 四子棋、街机生存、太空经营；固定原源码、完整首选源码和逐款许可，不是简化占位或外站 iframe。目录从 21 增至 27 个入口，新增六款精选筛选。

按需加载的静音工作稿支持小窗/放大、暂停/继续/结束与便签遮罩，失焦/隐藏/遮罩冻结本轮，主动恢复；仅本轮内存，不接办公币、正式奖励榜、网站游戏成就或本站房间。沙箱只 allow-scripts，Nginx 仅六个已审阅文档允许本站嵌入且禁止连接，原站点外壳禁止嵌入仍保留。217 文件 / 1900 前端测试、类型/社区构建、新鲜备份完整恢复与离站校验通过；同一镜像六款匿名实操及代表窄屏检查、最终 65 项公网烟测与 100 文件哈希通过。认证 Cookie 检查跳过，不冒充全部关卡、真人手机或长期平衡验收。[第三阶段发布](RELEASE_GAME_PACK_20260915.md) · [玩法、许可和重编译](LOCAL_GAME_LAB.md)。

## 后续阶段（尚未交付）

1. 正式后端与更多游戏：本批六款本地接入已交付，接入正式成绩、真人房间或奖励须另有完整规则与防刷验证。后续再逐款审查新的开源项目，不暗示索引中的所有游戏均已获统一授权。
2. 其他业务页：依据实际使用逐页统一布局、错误/空状态和手机操作，不给全站常驻重型特效。

## 开源边界

ThreeUI Community 的邻近弹簧导航算法按 MIT 许可局部改写，固定上游提交 `68802d5428071ada5c20db8094b1649e6bb770ed`。第一阶段主应用没有安装整包、第二份 Three.js、Pro/Beta 或远端图库；第三阶段 Server Survival 在独立隔离游戏内发行本地 Three r128，不替换或混入主站新版引擎。不嵌入第三方站点，第三阶段只增加六个自托管游戏文档的确切嵌入例外，其余 CSP 不变。源码归属见 `third_party/threeui`，Web 发行物含 `/third-party/threeui-license.txt`。

`bobeff/open-source-games` 是索引，不是所有游戏及素材的统一授权；新增每款都需独立核验。暂停的掌心 AI 工作不包含在第一、第二、第三阶段发布中。
