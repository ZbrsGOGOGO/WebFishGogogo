# 赵云救阿斗源码版发布记录（2026-09-19）

状态：**应用代码已推送 GitHub `main`，生产 Web 已上线，反馈已按实际交付完成**。应用提交为 `cbc17e54d2208aec5d0043aad94ca4a04d4b25c5`，北京时间 2026-09-19 17:05:24 完成 Web-only 切换；本记录的后续纯文档提交不重建应用。

## 实际运行版本

| 对象 | 提交 / 镜像 |
| --- | --- |
| Web 应用 | `cbc17e54d2208aec5d0043aad94ca4a04d4b25c5` |
| Web 镜像 | `sha256:e620e862475e9a1d744f901bc3c170a8fc0170b4578ad9db06929a67b53a0ee7` |
| API 应用 | `ef93db12d3a900e3e3506e5d595a6b9f5d7e813e`，本批未重启 |
| API 镜像 | `sha256:90ce0e1a1907822bf5aae8d35e581811e5599d163e8b9eedb863cd7a86cca958` |

数据库保持 schema40 / 41 条迁移 / 150 张 public 表。本批没有 API、迁移、Redis、Gateway、数据卷或经济规则变更；API、PostgreSQL、Redis、Gateway 容器完整身份在切换前后保持不变。

## 范围

- 站点所有者提供 `jiuceng.zip`，归档 SHA-256 为 `e2ec90da0f0429e3fa022db787481817d5135c3964b1523c599f358e6435b21d`。
- `/tower-defense/word-front` 改为同源嵌入归档中的 `zyjad` 浏览器玩法：原版 8×10 棋盘、武将/武器、章节、无尽、自动出兵、图鉴和神秘商人均保留。
- 旧 V4 引擎及服务端验真榜保留在 `/tower-defense/word-front/v4`，玩家房间保留在 `/tower-defense/word-front/rooms`，可独立回滚和继续使用。
- 仅导入 MIT 浏览器资源和属性表；不导入若依 Java 管理后台、SQL、固定 `guest` 身份、内存排行榜、编译产物或 `.git`。

## 现网适配与安全边界

- 源码存档键绑定本站 `publicId`，避免同一浏览器切换账号后串档。
- 原源码客户端“办公币”改名为玩法内“战利券”，不读写或冒充本站统一钱包。
- PVP 按钮进入本站已有服务端房间；排行榜按钮进入保留的 V4 服务端验真赛季。
- iframe 为本站同源静态页面，CSP 禁止联网、表单、对象、媒体及第三方资源，只允许本站脚本/样式；Nginx 只允许该精确文档被本站同源框架加载。
- `postMessage` 只接受同源且来自目标 iframe 的消息，导航路径限制在文字塔防子路由。

## 验证

- 导入的 8 个 JavaScript 文件全部通过 `node --check`。
- 新增源码/CSP/账号隔离/导航测试，专项 6 条通过。
- 前端全量：223 文件、1942 测试通过。
- TypeScript 类型检查通过；Vite 生产构建通过。
- 本地浏览器实测大厅、章节选择、首关 8×10 棋盘、首抽、倒计时和自动战斗正常。
- 最终不可变镜像通过 Nginx 配置、八个页面/资源入口、同源嵌入策略、禁联网 CSP 与源码哈希预检。
- 公网社区烟测 67 项通过；13 个赵云源码文件逐个与提交及运行镜像哈希一致，默认、旧 V4、房间三个路由均返回 200。
- 线上真实 Firefox 依次进入源码大厅、章节选择和第一关，确认 8×10 棋盘、手牌、包子、阿斗血量、20 波及自动出兵界面。未使用生产玩家凭据，外层受保护页面只验登录门禁，不冒充完整认证存档验收。

## 发布与回滚

- 本次为 Web + Nginx 静态策略发布，无 API 代码、数据库迁移、Redis、网关或数据卷变更。
- 发布前新鲜备份位于生产限权目录 `/opt/webfish-backups/zhao-source-20260919.irZzWp`；远端与离站原始归档 SHA-256 为 `bfcce0b84f8b64c93bca95a55258bc523d479585e5f28c1dd3e462effe4cdecc`，离站 AES-256 加密归档 SHA-256 为 `c7da60f0c34d71f45446aae20fc40e9202364f298d769275d469634d256cbb20`。完整 dump 在无网络 PostgreSQL 16.14 中恢复为 41 条迁移、150 张表、0 个未验证约束。
- 直接回滚只恢复发布前 Web `3e32257d4bc46329482c7ba3d4cacb5dd996abb2`、镜像 `sha256:b9dee18c35041ccefa15c47354dea0844ac3c58deacfb32b5d9cfaac836457e2` 及切换前环境，使用 `--no-deps --no-build --pull never --force-recreate --wait web` 仅替换 Web。API、房间、数据库、Redis、Gateway 和数据卷不动，不执行 DOWN 或旧库回灌；保留的 V4 路由不依赖本次源码资源。

## 协作反馈

上线后重新读取 `b512671c-cba1-40a9-8a45-d54de9c830d6`，确认自 2026-09-17 最近评论后没有新补充。先将十个分项按实际交付回填为 10/10，再以站长明确授权的运维完成入口标记为 **done v6**；两条审计均绑定应用提交 `cbc17e5`，作者通知已入队。最终全站为 **34 条：32 已完成、2 进行中、0 待审阅**。旧塔防与九层妖塔两个大型提案仍进行中，本批没有混入其未完成代码或改变状态。
