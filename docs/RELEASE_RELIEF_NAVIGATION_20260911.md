# 压力整理与统一工作区导航：2026-09-11

## 已上线

- 应用 **`a84759f9caae25fb15fa0ffb756af1f8ccd437ee`**，北京时间 **2026-09-11 17:02:37** 上线。
- GitHub `main` 已正常快进同步应用代码，未强推；后续公开记录属于文档提交，不重建应用。
- API 镜像：`sha256:32fa533620aa250479b1f499fafecc6dccfac9052352f67f18bffe2f1ffb5911`。
- Web 镜像：`sha256:b3a1f1a5969923b8cf537a44b20e7446683b2aa4c3e389da1209d1f8b263c4c9`。
- 部署前生产与直接回滚基线为 **`9413977c973e1146a9c70ef02875251e2a2ffbd2`**。
- 保持 **schema36、37 条迁移、148 张 public 表**；没有新增 SQL 迁移，也未执行生产 UP / DOWN。
- 只切换 API / Web，环境仅 `IMAGE_TAG` 变化；PostgreSQL、Redis、Gateway 完整容器身份及生产数据卷不变。
- 原投稿、审核和妖塔开关保持；本批未新增账号权限、真实支付或重复赠送权益。

## 压力整理：仅免费版

入口为「小游戏 → 压力整理」`/games/office-boss`，同页切换「机会挑战 / 每日巡视」，另有待领物品、外观与称号、记录和规则子页。原 `/office?tab=boss` 保留。完整概率、额度与资产说明见[免费挑战规则](OFFICE_RELIEF.md)。

- 每 1,800 秒新认可活跃时间获得一次机会，最多保留 10 次；机会和余数不按日清零，满池停止积累、不囤溢出。
- 复用账号级活跃心跳、标签租约和序号去重，每日最多认可 14,400 秒；不按旧历史时长回填，游戏时长不重复计入。
- 页面隐藏、失焦、闲置时暂停客户端心跳；协议防重复与任意自报秒数，但不是不可绕过的真人活跃证明。
- 手动消耗机会，服务端生成结果；没有自动挑战、付费购买机会、会员倍率或支付接入。
- 绑定「解压币」只购买本模块六款固定价外观，不充值、转账、交易或兑换办公币，亏损最多扣到零。
- 三款稀有称号写入真实所有权，由用户主动佩戴，沿用个人主页和聊天展示，不自动替换原称号。
- 妖塔材料、种植经验礼盒和研习册进入待领区，明确领取后同事务进入真实模块；条件不足时保留。
- 原每日巡视保持独立：30 秒后每日一次领取 **20 办公币 +5 职场经验**，不消耗机会、不因点击强度加奖。

机会扣减、独立代币、跨模块奖励、称号和回执在同一事务中提交。请求要求 UUID、严格字段与显式版本；同请求重放返回原结果。结果不确定时只核对原请求，不通过刷新重抽或重复扣减。有效账号、开关或最终投影检查失败时整体回滚；不改其他账号或管理权限。

## 真实验收发现并修正

最初候选 **`5cf77b5` 没有上线**：农场礼包写入旧 `farmCoins`，该余额早已停用，实际种植界面没有可用收益。修复提交 **`525df4e78adbfbe9c6e7189241a9bb679c2e25e0`** 改为真实 `plant_experience` 每份增加 **30**，按既有 `farmLevelSnapshot` 更新等级、自然解锁及农场版本。

修复遵守既有经验容量与等级上限，不改变旧农场币、作物、收获次数、订单或全站钱包，不伪造一次收获。验收实际调用 `DeskPlantService` 投影，确认经验和等级可见，另验证容量不足保留礼包、失败回滚与重复请求不多发；不再只检查未使用的原始字段。

音效默认关闭，仅用户明确开启后对新成功挑战回执播放本地短音。浏览器音频启动挂起时 **3 秒**后释放并静音降级，视觉反馈和挑战继续可用；离开模块、切号、隐藏、失焦或工作遮罩出现时停止，不补播。无需外部音频服务、麦克风或真人肖像，演出遵守减少动态效果设置。

## 统一导航与本浏览器自定义

- 社区工作区的统一目录覆盖工具、游戏与公开页面，保留完整目录搜索和快速切换。
- 当前账号可排序、隐藏和重置模块，**仅保存于当前浏览器**，不承诺跨设备云同步。
- 只接受预设稳定模块 ID，不允许任意路径、外部链接、脚本或自定义代码；新增模块会补入目录。
- 隐藏不改变权限；可见性实时结合功能开关和账号身份，切号与同账号新会话不沿用旧权限响应。
- 跨标签修改检测编辑冲突；存储损坏或不可用时回退默认，不静默覆盖无法读取的旧数据。
- 不修改原工作台收藏；明确重置只处理当前账号的导航偏好。
- 打开、关闭或折叠目录不重开当前游戏；实际跳页仍按原游戏离页规则处理，输入隔离和工作便签遮罩边界保留。遮罩不隐藏浏览历史、网络访问或取消已提交操作。

## 分层验收

- 最终前端 **181 文件 / 1,468 项**通过。后端在 `525df4e` 完成 **156 套 / 1,538 项**；另 **5 套 / 40 项**条件跳过，未计入通过数。最终 `a84759f` 的 backend/shared 代码树与该后端回归版本完全一致，已核对树身份，不冒称全量后端重新执行。
- 全仓类型检查、完整构建及最终不可变 API / Web 候选构建通过；没有用临时覆盖源码或 dist 代替最终镜像验收。
- 最终真实 PostgreSQL **12 项**通过，覆盖同 UUID/不同版本竞争、心跳与消费串行、真实资产领取、末端故障和事务回滚。
- 真实服务专项 **8 组**通过：严格动作边界、有效账号与开关、机会计时、解压币、真实称号、农场/妖塔联动、重复领取及原每日巡视。
- 旧不可变 `9413977` 兼容探针 **6 组**通过：旧模块和全部迁移可读、旧妖塔常规动作保留物资和品质经验、旧 Office 动作保留新 relief JSON、真实种植经验投影、称号所有权保留及其他账号不变。
- 压力整理最终浏览器 **13 组 / 59 张截图**通过，覆盖真实登录/HTTP、机会确认、原始服务端随机结果、固定价外观、待领物品、真实称号、每日 30 秒奖励、切号与窄屏深浅色。
- 浏览器后置 PostgreSQL **6 组**通过，逐一核对实际操作回执、单次礼包到账、钱包与经验、主手品质经验、称号和另一账号隔离，不用界面文案替代持久账目。
- 统一导航最终浏览器 **8 组 / 31 张唯一截图**通过；其中 **18 项矩阵**覆盖 1440 / 390 / 320px、深浅色下的目录、设置与游戏工作区。保存/隐藏/排序/重置、跨标签冲突、切号、权限、工具/公开页面与游戏遮罩均有验证，测试前后资产快照不变。
- 公网 **57 项**通过，**5 个实际静态资源**逐一与候选哈希一致：最终 HTML、入口 JS、压力整理 JS、主 CSS 和主题脚本；主要深链接及三个健康接口正常，受保护个人 API 和匿名写入均拒绝，妖塔公开目录仍开启。

以上是有范围的分层验收，不表示全站每款小游戏、所有第三方来源或任意并发容量重新通过，也不把条件跳过算作真实数据库测试。候选浏览器使用隔离合成普通账号；强制稀有分支只用于隔离服务测试，实际浏览器挑战结果未篡改。

## 开发协作回填与保留待办

上线及公网检查通过后，先重新读取提案与完整事件/评论，再按精确对象、版本、正文及事件摘要执行正规进度记录和完成操作。每条先完成真事务回滚校验，再提交并独立只读复核，没有重放旧权限或旧提案。

- 压力整理：**已完成，记录版本 1 → 3，8/8**，指本站已验收的免费适配范围。
- 统一侧目录：**已完成，记录版本 1 → 3，2/2**，限定当前浏览器内的预设模块排序、隐藏和重置，不代表任意模块开发或云同步。
- 每条新增 2 条系统审计与对应 2 条作者系统通知；没有伪造用户评论或重复回填。
- 导航首次私有回填预演因通知证据字段检查不匹配而失败；全快照比对证明事务零残留，修正取值后重新通过完整回滚与正式回填。这是运维验收脚本修正，不是生产应用故障。
- 北京时间 **17:13:06.356** 全量只读复核共 **23 条：21 已完成、1 待审阅、1 进行中**。相对 17:03:29 基线只改变本批两条，没有新提案、补充评论或附件变化。
- 新工位塔防四点仍为 **待审阅、记录 v2**：每章起手、已放置工具移动、左右塔位和天赋保存，本批仅诊断、未实施。现有规则与交互缺陷已区分，后续需要独立开发和验收。
- 九层妖塔 v0.11 仍为 **进行中、记录 v2、4/11**，保留 7 个后续待办；不把整份未来规划或全部协作标为完成。

## 备份、回滚与留存

新鲜生产备份完成隔离恢复和 148 表核验，**7 项生产备份摘要**通过本机与异机校验。合成数据库和完整验收证据归档后，**8 项合成归档摘要**亦通过本机与异机验证；生产副本与合成测试库独立，不把测试统计当成生产玩家数据。

直接回滚目标为 **`9413977c973e1146a9c70ef02875251e2a2ffbd2`**。先停止新版 API / Web 写入、等待在途请求退出，再恢复本批旧环境及旧不可变应用镜像，仅启动 API / Web。兼容探针已验证旧版保留新增 relief JSON、真实种植经验、奖励、回执及称号所有权；旧投影可能不展示新称号，但不删除所有权。

**本次回滚无需关闭妖塔或自动探索**，不能套用此前 `9413977 → 3426077` 的旧妖塔暂停措施。保留全部新旧存档、钱包账目、帖子、反馈、审计和奖励；不回灌旧数据库、不删新 JSON、不重放发奖或权限、不执行 DOWN。回滚脚本已准备并校验，旧镜像隔离兼容探针通过；本轮没有实际回滚生产。

归档门禁通过后，按精确 ID 和任务归属清理了 **5 个合成测试容器及其独立内部网络**。生产容器、数据卷和真实账号未动；归档、镜像、源码与证据保留。凭据、环境备份、私人反馈原文和附件限权留存，不进入 Git。
