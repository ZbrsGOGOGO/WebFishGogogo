# 成员投稿与站长审核：2026-09-10

## 已上线

- 应用 `2111004be1cc407b95cc9859afc43ac8fb10ece5`，北京时间 **2026-09-10 20:30:17** 切换，GitHub `main` 已正常快进同步。
- API 镜像 `sha256:e58bf403bdbd9cc37de73d6b016122f15c4502d2a9afcfbb92c1884bbad6ea5a`。
- Web 镜像 `sha256:d972ade3ed7c101b1678b1fd29ff983579a5b9c56036adc3d770d1ac0395c790`。
- 前端及内容服务改变，无协议/依赖/数据库结构变化；schema36、37 条迁移、148 表不变，没有执行生产迁移。
- 仅替换 API/Web；环境仅 IMAGE_TAG、`FEATURE_COMMUNITY_CONTENT_WRITES_ENABLED=true`、`FEATURE_COMMUNITY_MODERATION_ENABLED=true` 变化。已核实运行 API 对应的写入/审核开关均为 true，PostgreSQL、Redis、Gateway 容器 ID 保持不变。

## 用户可见变化

经验交流 → 帖子与问答增加固定的「发布帖子」及空列表「写第一篇帖子」。只读、加载失败等情况说明原因，不再把入口隐藏。管理员在帖子页可点「帖子审核」，也可从页头或全部栏目进入审核台。

按站点所有者本次明确确认，**active 登录账号均可创建、编辑自己的帖子并提交/撤回审核**，不再要求额外社交核验。所有帖子，包括站长帖子，必须人工审核；待审修订不会覆盖已批准正文。未登录、停用/封禁、作者归属、写入开关、幂等和版本冲突检查仍生效。

zbrs 发布前只读核验为 active/admin，未社交核验；本次沿用其已有管理员角色，没有更新真实账号的角色或核验字段。普通账号不能调用审核 API，开发协作权限也不等于内容审核权限。其他模块及评论原有核验规则未在此任务中放宽。

审核沿用通过、限制展示、隐藏和恢复，必须填写原因并保留审计；“隐藏”是帖子的展示处置，并非新增的驳回修订功能。详见[投稿与审核说明](POSTING_AND_REVIEW.md)。

## 实际验收

- 前端全量 **172 文件 / 1,261 项**通过；本次内容模块 **4 文件 / 13 项**包含其中，不重复累计。后端内容服务与开关回归 **2 套 / 20 项**通过；前后端类型检查、完整构建通过。既有大包警告仍在。
- 新增覆盖未核验正常账号投稿、撤回/修订、待审隔离、作者限制、普通账号无审核权、管理员也不能自动发布，以及五种非 active 状态不能创建草稿；前端覆盖固定入口、角色入口、只读提示、切号旧响应隔离、未核验账号保存提交和非作者编辑禁用。
- 最终不可变镜像使用全新合成 PostgreSQL、内部网络和三个合成账号（投稿成员、管理员、另一名普通读者，均未社交核验）。完成 **6 组工作流、14 张唯一 Firefox 155.0.1 截图**，实际 320/390px 宽度和 SHA256 跨机器核验。
- 真实 HTTP/数据库验证草稿幂等、私有待审、他人不能改稿/审核；浏览器原生登录、填写并提交帖子、站长选择案件/填写原因/审核通过、另一普通成员读取正文均通过。后续修订仍保密，审核版本冲突返回 409，隐藏与恢复不提前公开未审正文；手机编辑/审核页无文档横向溢出。普通成员不能进入审核界面。
- 一次浏览器脚本切号等待不足导致验收中止；补齐页面等待后重新完整通过，没有在失败时切生产。抽查了最终发帖入口、审核台和 320px 编辑画面。
- 新鲜加密生产备份在无外网克隆完整恢复；148 表摘要、结构、索引、约束、触发器、序列及完整迁移历史只读核验通过，候选实体映射正常且无待执行迁移；异机备份校验通过。
- 公网 **25 项**通过：HTML、主题脚本、入口 JS/CSS、桌宠模块哈希匹配；页面、健康正常，帖子及审核权限 API 匿名均 401。公网 Chromium **6 项 / 3 张截图**验证 1440/390/320px 下游客进入投稿和审核均先登录，页面不溢出。
- 没有用真实 zbrs 会话写测试帖子，没有给真实成员新增权限，也没有用生产数据冒充隔离用例。本轮不宣称所有小游戏和实时聊天重新逐项验完。

## 回滚与留存

直接回滚应用 **`49071c8452e1749423d3b58c88c8c59677a54d03`**，恢复本批备份的完整旧环境（写入和审核开关均为 false）及旧镜像，仅切 API/Web。保留所有新旧帖子、修订、审核审计、用户资产和 schema36，不执行 DOWN 或旧库覆盖，不重放赠送/权限。本次未回滚；API 重启仍会中断临时内存游戏房间。

真实加密备份、合成库及验收证据分开限权留存，不进入 Git。合成环境为 3 个测试账号、21 项钱包记录、余额合计 1500、0 条支持台账、3 条成长记录，均来自隔离测试。备份及异机校验后按标签/ID 清理本批 5 个测试容器与内部网络，数据可从合成备份恢复；未删除任何生产卷或真实用户数据。本机独立验收浏览器在交付时停止。后续发布文档提交不代表重新部署应用。
