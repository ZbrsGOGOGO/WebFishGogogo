# 工作台统一界面：2026-09-10

## 已上线

- 应用 **`7dd04d23a0e094e8e542d46bb54feb80a2d92eea`**，北京时间 **2026-09-10 17:55:17** 上线，GitHub `main` 已正常快进同步。
- API 镜像 `sha256:0cb4fff40858babf98f34ee69c2fd254f2d3c26e04b7f91949d364be5b76d82f`。
- Web 镜像 `sha256:5ffca7bd583faa98be102dc6c11818624e1935e91cc7a39a7c220e9169b33112`。
- 仅前端代码及说明改变，后端、共享包、依赖和部署运行配置没有代码差分。沿用现行发布方式替换 API/Web；完整环境仅 IMAGE_TAG 变化，PostgreSQL、Redis、Gateway 容器 ID 不变。
- schema36、37 条迁移、148 张表保持不变，未执行生产迁移、赠送或成员授权。后续文档提交不代表再次构建部署。

## 界面变化

全站公共组件统一浅色语义变量：白底、浅灰分区、细边框、绿色主按钮、蓝色链接和键盘焦点。首页、侧栏、工作台、好友/聊天、资讯、开发协作、成就、桌宠设置、工具与游戏外围界面收敛圆角、阴影、字号与间距；场景、角色、武器、稀有度和桌宠插画保留。

新增内置 SVG 导航图标和「全部栏目」搜索目录，匹配名称及简介。手机可随时跳转工具、游戏、榜单及账号安全；搜索空状态、Esc、焦点圈定/恢复、跳转关闭和切号关闭均覆盖。开发协作及管理入口沿用原权限，没有给真实账号增加角色。首页使用「你的日常工作台」，不改变玩家称号及游戏内部规则。

详见[界面规范](INTERFACE_DESIGN.md)。不包含新深色模式、付费服务或图像生成依赖。

## 实际验收

- 前端全量 **169 文件 / 1,236 项通过**；最后补充按栏目简介搜索后，受影响的布局和配色 **2 文件 / 24 项**再次通过，不累计重叠数量。前端类型检查及显式 community 构建通过；既有大包警告仍存在。
- 配色测试覆盖主要文字在白/浅灰背景的对比度及主按钮、焦点色。不能据此宣称所有游戏画面和历史语义色都完成无障碍认证。
- 开发态 Chromium：5 类页面 × 4 宽度（1440/1024/390/320）检查布局，并检查目录弹窗；未发现文档横向溢出。
- 最终不可变 API/Web 镜像、独立内部网络、新建合成 PostgreSQL 和两个合成账号（普通/管理员）：Firefox **155.0.1** 完成 **8 组检查、31 张唯一截图**。原生登录、真实余额 500、工具筛选/打开/输入、目录搜索和 Esc/焦点恢复、普通账号无协作入口、管理员协作界面、桌宠点击/喂食、游戏棋盘和便签遮罩均通过。
- 登录态桌面覆盖好友、私信、聊天室目录、新闻、成就、小游戏、排行榜及桌宠；320/390 CSS 像素覆盖首页、工作台、聊天室目录、新闻、桌宠、游戏和目录跳转，另检查 320px 管理界面。截图 SHA256、实际 PNG 宽度跨机器复核。抽查了工作台、聊天、窄屏、开发协作及公网首页画面。
- 本轮聊天/新闻/开发列表使用合成环境的只读或空状态，**没有据此声称重新完成真实双人聊天发送、全部小游戏对局、新闻来源同步或上传处理测试**。原有相关前端回归通过；未用真实成员账号写测试数据，也未读取真实协作私有附件。
- 生产新鲜加密备份完整恢复到无网络克隆；候选实体映射、无待执行迁移、全部 148 表内容摘要、结构、索引、约束、触发器、序列及完整迁移历史核验通过。备份异机 SHA256 校验通过。
- 公网 **19 项**通过：HTML、入口 JS/CSS 和桌宠懒加载 JS 与最终镜像哈希一致；健康、页面入口正常；今日流水、私人成长档案及支持后台匿名均 401。另在真实 HTTPS 站点用 Chromium 验收首页、工具及登录页的 1440/390/320 布局和目录弹窗，无文档横向溢出。响应式桌面浏览器不冒充实体手机。

开发中修正了配色测试的 CSS 读取方式和浏览器脚本的选择器转义；候选失败未切流，最终重新跑通。备份恢复曾被仍在构建的发布锁拒绝，构建结束后才顺序执行，没有绕过发布锁。截图传输的一次输出混杂被拒绝，重新传输并逐项校验成功后才部署。

## 回滚与留存

直接回滚基线 **`57345dd5dbe510fc74f771e887f61afaa1ef8816`**，使用保留的旧镜像及本批完整环境，只替换 API/Web。保留 schema36、玩家数据与本机桌宠图片，不执行 DOWN 或旧备份覆盖，不重放赠送、不关账号注销。回滚同样会结束临时内存房间；本次没有执行回滚。

真实加密备份与合成验收材料分开限权保存，不进 Git。合成管理员只存在于新建的隔离数据库，不能将该合成库恢复到生产。

合成库留存 2 个测试账号、14 项钱包余额记录、余额合计 1000（既有开户逻辑）、0 条支持台账及 2 条成长记录。合成数据库、运行记录和日志异机校验后，按实际标签、容器 ID 与内部网络逐项清理本次 5 个测试容器及网络；数据可从留存合成备份恢复。生产健康复核正常，没有删除真实用户、生产数据卷或玩家数据。本机临时预览服务与独立验收浏览器也已停止。
