# 2026-09-08 分类新闻与官方榜单入口发布

## 发布结果

- 应用提交：`cbff5100be7c26457d3768e43da7e6153a2fc306`，已推送 `feat/workstation-tower-defense`，远端引用核验一致；未合并 `main`、未强推。
- 生产 API/Web 已运行同一提交，健康检查通过；本版包含此前已上线的农场收益和余额修复。
- 回滚应用：`cde4e54e0e6781cf82eb456290e296ed6af065a2`。回退应用不回退数据库、钱包、反馈或已有新闻快照。
- 无迁移、权限调整、新增服务或付费依赖。详细规则见 [分类资讯说明](NEWS_CATEGORIES_2026-09-08.md)。

## 本次功能

- `/news`：综合、国内、国际、社会、财经、文娱、体育 7 类 RSS 标题，支持分类筛选与 URL 状态；保留来源和原始发布时间，只链接原文、不转载正文，分类不是热度排名。
- 中国新闻网官方 7 路 RSS，每类最多 8 条，目前最多 56 条；跨类标题和 URL 去重。抓取中提供旧快照，任一源失败保留旧数据并重试，全部成功才原子替换。
- 固定源白名单、3 路并发、8 秒超时、XML 类型检查、2 MB 限长、禁止跟随跳转；数据库锁与随机租约身份防止并发和过期任务覆盖。
- `/news/trending`：微博、知乎、百度、哔哩哔哩、抖音、豆瓣、GitHub、Hacker News 共 8 个官方入口，分综合社交、视频文娱、科技知识三组。明确标注“站内未同步榜单条目”；不伪造排名、热度或更新时间，部分来源可能要求登录或安全验证。
- `/news/editorial`：保留人工导读、职业/主题筛选和明确启用的偏好。三个栏目和原详情入口继续受成员登录与功能开关保护。
- 移动首页新增分类新闻与榜单入口，不占用底部社交导航。

## 验证

- 后端 **97 suites / 517 tests**、前端 **108 files / 631 tests**，共 **1,148 项通过**；最终全仓类型检查、完整本地构建和生产候选镜像构建通过。
- Firefox 桌面与真实 390px 手机视口通过，手机文档宽度 378px，无横向溢出。覆盖分类返回、榜单筛选、8 个安全外链、导读偏好、移动首页入口、503 重试及全局余额入口；浏览器使用合成 API，不冒充线上新闻。
- 候选镜像的隔离 PostgreSQL 验收全部通过：旧 14 条快照升级到充足夹具的 56 条；抓取中读旧、失败保旧与重试；跨类去重和原始日期；同日幂等；6 个新日期并发请求仅 1 个提交、合计 7 次源请求、峰值并发 3；过期及同毫秒不同租约不能覆盖新任务；跳转和超限响应不能替换快照。
- 隔离测试仅操作初始为空的自有 `2099-06-01/02` 夹具，清理后原用户、钱包、农场、开发反馈、新闻及迁移等 11 表行摘要相同；临时容器已精确移除。
- 真实 HTTPS 专用普通测试账号：登录、安全 Cookie、Origin 拒绝、刷新轮换、开发界面拒绝越权、新闻分类/人工导读/偏好接口通过；测试账号及关联数据精确清理。
- 线上真实新闻：服务日期 `2026-09-08`，快照更新于北京时间 **11:05:49.539**，7 类各 8 条，共 **56 条**；全部为 `chinanews-v2-*` 来源，HTTPS 原文和 72 小时内原始发布时间核验通过。该数量是本次实际结果，不保证每天固定数量。
- 真实 HTTPS 农场回归通过：第 4 次仍有基础办公币、订单加成归零、重复请求不重复入账、续种扣费正确；低余额草莓用例为 `15 + 567 - 240 = 342`，工具升级后余额 `142`，查询一致。
- 原公共烟测从生产服务器经 HTTPS 域名执行 **55 项 HTTP 检查通过**；认证业务另由上述专用账号完整验收，不依赖公共脚本默认跳过的认证段。本机两次烟测遇到不同请求的 TCP 连接超时，原失败日志保留，未弱化检查。
- 线上入口 `/assets/index-6JwCvDum.js` 与两个新路由核验通过，含分类、榜单外链说明和原农场余额；塔防 chunk `workstation-tower-defense-DviVZY_O.js` 保持不变。

## 首轮拦截与修复

- 首候选 `6290a94` 在隔离数据库的 6 并发用例被拦截：虽只最终写入一份，但多个任务重复请求来源。
- 根因是新日期使用 TypeORM `save()`，二次存在查询可能转为更新并覆盖其他事务刚插入的租约。最终改为新日期严格 `insert()`；已有行才在 `FOR UPDATE` 锁内更新，唯一键失败者不抓取，并新增两条回归。
- 首候选未切换生产 API/Web；准备阶段的 checkout/IMAGE_TAG 已恢复农场版，没有重建运行容器。失败证据保留在 `news-categories-20260908.8mBzfO` 服务器及异机目录。新版重新完整验收，未降低标准。

## 备份与部署边界

- 本次成功发布目录：`/opt/webfish-backups/news-categories-20260908.Gh0axK/`。
- 异机备份：`/home/zbrs/.local/state/webfish-backups/news-categories-20260908.Gh0axK/`。
- 新鲜 AES-256-CBC / PBKDF2 加密 PostgreSQL 备份、完整隔离恢复和异机逐项校验均通过；清单绑定密文、密钥、原环境、版本及运行身份。目录 700，环境/密钥/密文 600；秘密和私有日志不入 Git。
- 隔离验收不提供生产凭据、不发布端口、不访问真实 RSS。门禁通过后仅用 `--no-deps` 更新 API/Web。
- PostgreSQL、Redis、Gateway 容器身份不变，迁移仍为 `1700000000026`；未删除或重建数据卷。

## 反馈与最终核对

- 农场收入成本反馈已为 `done`，版本 2；微博热搜反馈仍为 `submitted`，版本 1，因为官方入口不等于站内实时热搜聚合。其他模块建议未混入本次发布。
- 最终只读核验为 11 个普通用户角色、4 个有效开发协作成员；新闻验收账号残留为 0，未扩大任何账号权限。
