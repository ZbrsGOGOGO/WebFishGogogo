# 文字战线 v2 与协作反馈发布记录（2026-09-14）

## 交付边界

- GitHub `main` 应用提交及生产运行提交：`994b4797e561901297cdebc0dfbad6c466d81cd0`；基础功能提交 `f8b85140fa75dd9c2b038c8edf36a87600360834`。北京时间约 15:28 完成 API/Web 切换。
- 独立文字战线 v2：六章路线、五类来客、双字角色、地形与仅局内金币商人；独立 `word_story_v2` / `word_endless_v2` 榜。原文字战线 v1 和原工位塔防保留，不接账号办公币。服务端固定种子、章节和动作回放结算。
- 妖塔：真实战报评级、每日任务进度、皮糙肉厚与无影手两项免费被动；档案皮肤布局和颜色转换 CMYK 整数显示修复。无付费、权限、赠送、每日奖励或办公币调整。
- 两项大型提案仍在进行：妖塔 6/11，塔防 4/9。真人房间、局外经济、魂珠、其他技能、周期商店、活动升星与正式替换均未宣称完成。原案的招募费用、字池权重和组字规则相互矛盾，后续须另行统一。

## 验证与数据

- 本机完整 `npm run verify`：后端 159 套 / 1586 项通过，5 套 / 57 项既有条件跳过；前端 194 文件 / 1667 项通过；类型检查、完整构建、社区模式构建和 `git diff --check` 通过。V2 窄屏顺序、触控格和重复字卡读屏标签另有定向测试。
- 新鲜生产备份经加密、校验并复制到另一机器；切换前档案 SHA-256 为 `945ee9e7ff1929c9d031ecff585c84139cfa5c6cc57d3779f9ea1b332add2ffd`。较早同批备份已完整恢复到隔离 PostgreSQL，原数据与迁移起点核对；不在仓库保留环境、备份、密码或真实附件。
- 隔离副本从 38 条迁移升级到 39 条：唯一增量 `1700000000038` 只放宽两张 Arcade 表的 CHECK；148 张业务表、原 245 条对局和 8 条最佳成绩数量不变，重复迁移显示 0 待执行。合成账号在隔离副本完成新版服务端回放成绩；兼容桥拒绝伪造 v2 成绩、可结算未带版本的 v1 局，并可读取/训练装备 s17/s18 的新存档。
- 生产迁移同样仅执行 `0038` 一次。新 API 镜像 `sha256:5872d32eeaa2574d819f83426f62c1ff8c47c7c42e4f14313ab796fcfb5bb046`、Web 镜像 `sha256:fb4202d04642135bbf3849c700e433c31ac88e190a4f7d3b5dbaba996f1d5809` 健康。PostgreSQL、Redis、Gateway 容器 ID 与发布前相同，卷未重建。线上为 schema38 / 39 条迁移 / 148 张表、原 245 条对局与 8 条最佳成绩；v2 两榜可公开读取。
- 公网社区烟测 63 项通过；匿名页面实测 CMYK 从 `#FC5531` 自动换算为 `0 / 66 / 81 / 1`。没有获准登录测试账号，未运行认证 Cookie 烟测、妖塔档案登录态多宽度目测或真实玩家平衡测试；不能把这些写成已验收。

## 回滚与反馈

- 直接回滚只用兼容桥 `1f50c07f543d51ffa6688d90ff87b12b805ff921` 的已保留 API/Web 镜像（API `sha256:904f8b61e76c868db08ac2e1df752372e2ff7c1a94149a3758646d9158b6fbf0`，Web `sha256:84c852a7ccbbeed828c6c5f5870f3a513295be9d5b23d7f22789b342b5359a2c`）及其完整环境。按 API 就绪再 Web 的顺序切换；保留数据库/Redis/网关和 schema38，不执行 DOWN、旧库覆盖或删除玩家数据。桥不接受尚未结束的 v2 局结算，待候选恢复后方能继续；裸 `9013da7` 不得作为直接回滚目标。
- 4 条反馈回填为独立审计并通知作者：两项大型提案保留进行中，人物档案和 CMYK 两项保留原完成状态并记录评论修复范围。最终总量为 29 条：27 完成、2 进行中、0 待审阅。档案登录态视觉复验单列未完成，不把 CSS 单测冒充真实浏览器验收。
- 本次未创建无人值守反馈监控、权限账号、真实付款或额外办公币奖励。
