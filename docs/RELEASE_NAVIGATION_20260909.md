# 小游戏与工具直达导航 · 2026-09-09

应用版本：`a456a8b27027a45a7ebd10c9a4f9efd2f8ca6696`。北京时间 17:26 完成 API/Web 更新，17:27 后公网资源及真实浏览器验收通过。GitHub 分支仍为 `feat/workstation-tower-defense`，没有合并或强推 main。本记录是发布后的文档补充，不改变应用构建。

## 交付范围

- 工作台侧边栏新增常驻“小游戏” → `/games`、“工具” → `/tools`，无需通过首页卡片寻找。
- 手机首页页头下方提供两个 44px 高的快捷入口；底栏原有项目不变。快捷条只显示首页，不增加群聊/私聊会话页头高度，不挤压输入区域。
- 保留工具和游戏大厅的游客访问，以及房间、妖塔原有账号守卫。独立游戏/工具布局、低调游戏小窗未改动。
- 导航按完整路径段和最长路径匹配，`/games/demon-tower` 仍识别为妖塔；不会误匹配 `/gamesmanship`、`/toolshed`。
- 协作筛查只读进行。17:06 与 17:26 两次生产快照的全部 15 条及事件、附件元数据、审计内容一致；未更改反馈状态、通知、账号权限或玩家数据。私人提案及综合筛查报告未提交 Git。

## 验证

- 前端 149 个套件 / 1,060 项、后端 132 个套件 / 1,110 项，合计 2,170 项全部通过。最终导航 17 项另单独复跑；全部工作区类型检查通过，最后布局调整后前端类型检查再次通过。
- 全工作区构建通过；最终不可变候选镜像再次完成生产社区模式构建及 Nginx 配置、静态文件可读性验证。仍有已有的大于 500 KB 游戏分块提示，本次不声称已消除该提示。
- 候选后端及 shared 的所有编译文件 SHA256 与生产基线逐一一致。无数据库、依赖、实体或迁移变更；未冒称为本次重跑真实 PostgreSQL 全部业务演练。
- 本地 Firefox 154.0.1：1440 桌面，390/320 同源窄视口，9 组导航/往返/无横溢出检查通过。接口夹具隔离，仅返回真实编译目录及游客 401，不能冒称生产后端验证。
- 公网新访客 Firefox：1440 桌面和 500px 独立响应式窗口，7 组入口点击、页面返回、真实游戏目录及布局检查通过。Firefox 最小窗口限制阻止公网直接测 390/320；未绕过生产防嵌入安全头。
- 公网只读检查 82 次 GET：63 项 JS/CSS/许可证资源与候选镜像逐文件校验一致，入口页面哈希正确；三个健康端点通过。私有反馈、个人妖塔和新闻接口仍要求登录；CSP、HSTS、nosniff 及默认禁止嵌入保留。

## 备份、运行状态与回滚

- 回滚基线：`6085298c42485067a3f17fca54fe9904ec05d063`。保留旧 API/Web 镜像。
- 发布前数据库加密备份已在独立 `network=none`、内存临时盘 PostgreSQL 容器完整恢复，136 张表、33 条迁移记录/最新 schema32 一致。仅清理该次恢复容器及其临时副本，加密源备份保留。
- 生产备份目录：`/opt/webfish-backups/nav-release-20260909.6QXZmq`；异机副本：`/home/zbrs/.local/share/webfish-backups/nav-20260909-a456a8b`。目录 700 / 文件 600，八项 SHA256 清单全部验证通过；密钥、环境和数据库内容不进入仓库。
- 仅 `up --no-deps` 更新 API/Web，均 healthy；PostgreSQL、Redis、Gateway 容器身份保持，schema32 与全部迁移登记不变。环境只有 `IMAGE_TAG` 变化。
- API 镜像：`sha256:dc7db70a55a52bd402eed5ecfaed822c32ab7e7cf301e1b0ca05fab5bf025cad`。
- Web 镜像：`sha256:01928f6650b2e11ac0cf60f8392fa5becca86f4d4766f0384403fc3ffaadf20b`。
- 本次专用回滚脚本允许部分失败后的 API/Web 缺失恢复，并始终仅重建 Web 以刷新 Nginx 的 API 地址；不恢复旧数据库、不执行 down、不删除数据卷。608 已兼容现有妖塔成长规则，不照搬更旧版本的关停开关步骤。脚本静态审查通过，本次没有实际触发生产回滚。

浏览器证据保存在本机私有目录 `/tmp/webfish-nav-browser-20260909.K3yTHo`、`/tmp/webfish-nav-public-20260909.ikLoe9`，部署及备份记录在上述受保护发布目录。未新增无人值守反馈监控。
