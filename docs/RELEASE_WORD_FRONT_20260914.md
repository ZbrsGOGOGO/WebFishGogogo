# 2026-09-14 协作反馈分期交付：文字战线与妖塔成长

## 实际上线范围

- 应用提交：`9013da770a51044965486efec9b201ae69d5f7e8`，GitHub `main` 已快进推送；生产 API 于北京时间 09:41:47、Web 于 09:42:00 启动。后续文档提交不改变线上应用 SHA。
- 新增独立 `/tower-defense/word-front`，以《赵云救阿斗》提案实现五字卡招募、赵云/关羽/张飞/诸葛双字组队、同阶合并、路线防守、三章剧情和 30 波无尽。原工位塔防及其存档、排行榜和办公币规则不动。本期不做房间对战、商店、真实付费或正式替换。
- 剧情与无尽使用独立 Arcade 游戏键；服务端生成字卡种子，回放有界动作、分数和合理用时后才记榜。本机恢复草稿不能继续参榜。该校验拒绝无法由规则到达的分数，不保证防住针对公开种子预先计算动作的自动化玩家；榜单不发办公币。
- 妖塔仅对真实升级增加物品掉落机会及下次升级保底，Lv5/10/15 补足不同武器里程碑。旧高等级存档不追溯补发，原探索四次保底独立；Lv120 的最后升级不会留下无法兑现的待保底状态。战报展示真实结算结果、有效伤害、经验材料、近期过程和保留日志；独立评级算法、录像及动画仍未做。
- `1700000000037` 只扩展 `arcade_game_runs`、`arcade_best_scores` 的游戏键约束。schema36 → schema37，37 → 38 条迁移，始终 148 张 public 表；不新增办公币来源、真实账号权限或支付规则。

## 核验与切换

- 最终功能代码全量后端 158 套 / 1577 项通过（5 套 / 57 项按既有条件跳过），前端 192 文件 / 1643 项通过；类型检查、完整构建及社区模式构建通过。手机端招募/棋盘/榜单顺序和最小棋盘触控面积另经定向测试与重构建。当前工具环境没有可用浏览器，不能把静态和 jsdom 核验说成真实手机视觉验收。
- 切换前新鲜生产数据库和完整环境做限权备份；异机 AES-256 加密副本与原始 dump 摘要一致。生产 dump 完整恢复到不开放端口的隔离 PostgreSQL 16.14，候选镜像仅执行 0037；迁移后 38/38、148 表，旧 Arcade 行摘要不变，第二次运行无待迁移。旧 `e183860` 镜像能读取升级后的迁移历史和旧榜。
- 隔离库中用候选服务真实跑通服务端开局、完整胜局结算、跨账号拒绝、再次开局使旧局失效。测试克隆的独立容器、网络及数据卷随后按确切名称清理，生产 PostgreSQL、Redis、Gateway 身份及数据卷未动。
- 生产仅运行一次 0037 迁移，随后依次替换候选 API、Web；API/Web 镜像 ID 分别为 `sha256:cda1dba1ba7d488c34d9292c1b86ceb41ceaf7fde10924b68f921eb8044acf36`、`sha256:d45911911458f7c44e05f51e43ae671335085acf98064b7118c989e4440ed66d`。三条本机健康检查、网站侧 63 项 HTTPS 烟测及新入口、两张空榜、匿名写入拒绝、实际懒加载资源检查通过。生产没有获准的合成账号凭据，未执行认证 Cookie/真实手机浏览器验收；本地公网烟测曾受链路超时中断，改从服务器侧完整通过。
- 上线后两条反馈均保持 `in_progress`：`塔防游戏大更改--赵云救阿斗` 为首期 3/8 项完成；`九层妖塔 养成与数值对齐设计底稿（v0.11 规划）` 为 5/11 项完成，战报表现仍进行中。全站反馈计数 29 条中 27 已完成、0 待审阅、2 进行中。进度通过审计化离线入口逐项记录，未将未做范围误标完成。

## 直接回滚边界

回滚应用为 `e1838603c80aafce459cb2d2ef8a3507f4b4678c` 及切换前保留的完整环境、API/Web 不可变镜像。仅回切 API/Web；保留 schema37、新 Arcade 成绩、全部妖塔状态、账户资产、投稿、反馈和审计，不执行 0037 DOWN、不回灌旧 dump、不删除生产卷。旧应用不认识新升级保底的兑现语义，因此回退期间须暂停妖塔手动写入和自动探索，向前修复/恢复兼容应用后再开放；旧用户成绩不会自动导入其他榜单。回滚也会结束内存中的纸上突围临时房间。

本记录只说明本批验证过的边界，不代表策划案的 PvP、经济、评级或妖塔其余 v0.11 条目已经完成。
