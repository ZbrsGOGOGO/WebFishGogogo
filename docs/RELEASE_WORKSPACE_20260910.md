# 我的工作台与桌宠增强：2026-09-10

## 已上线

- 应用 **`57345dd5dbe510fc74f771e887f61afaa1ef8816`**，北京时间 **2026-09-10 17:04:44** 上线；已正常快进推送 GitHub `main`。
- API 镜像 `sha256:bab6654388321aa100298adb5e7f6e8b56a86cfa013f505ae777bd4f2700221b`。
- Web 镜像 `sha256:891aa74b0c8ff95e55b296ff51d6220e46d1b414afe265ec6db0768562dd0011`。
- 仅 API/Web 切换，完整环境差分只有 `IMAGE_TAG`。数据库、Redis、Gateway 容器 ID 不变；schema36、37 条迁移与 148 表不变，未执行生产迁移、授予权限或补发赠送。

## 实现范围

用户批准优化清单 1、2、4、5、6；个人入口采用「我的工作台」。详见[功能、隐私和计时口径](MY_WORKSPACE.md)。

- 全局唯一活跃计时覆盖独立工具/游戏路由，路由、遮罩、小窗状态切换增加暂停边界；榜单和目录不算游戏活跃，小窗需正在运行且未收起。
- 桌宠身份切换不再重挂整个路由，保留工具草稿与计时器组件；旧账号图片和异步回调隔离。
- 本机图片缩放/裁剪、橡皮擦、撤销和重置；不同互动标记、拖动靠边吸附和文字输入避让。方向键保留精细移动，不被吸附锁住。
- `/me` 集中账户余额、今日办公币实际入账/支出、成长进度、工具收藏和最近使用。新增 JWT 保护的只读流水汇总，按北京时间零点划分，金额保持 bigint 精度。
- 图片与轻量偏好分开保存，旧格式惰性兼容；本机备份导出、预览确认导入和安全图片重绘，不上传、不增加付费服务。

工作台新增文案使用中性表达，不重命名既有成就头衔或用户主动佩戴的称号。未加入房间持久化、前端独立发布或容量压测体系；普通游戏的内部暂停状态尚未逐个统一，计时仍是页面活跃统计，不是精确对局计费。原来漏记的时长不补造。

## 实际验收

- 前端全量 **168 文件 / 1224 项通过**；末轮暂停边界修正后，受影响的 7 文件 **72 项**集中复跑通过，不将重叠数量相加。全仓类型检查及明确 `VITE_SITE_MODE=community` 的全仓构建通过，既有大包提示仍存在。
- 后端全量 **145 套 / 1369 项通过**；另有 3 套 / 23 项外置数据库测试按原配置跳过，不称为通过。
- 最终不可变 API 镜像对隔离真实 PostgreSQL 验证午夜含/不含边界、其他账号/币种排除、正负分离、`9007199254740993` 精确金额、停用/不存在账号拒绝。合成事务回滚并核对无遗留；不是在真实用户库做写入演练。
- 最终 API/Web + 全新合成数据库 + 两个普通账号，Firefox **155.0.1** 完成 **12 组真实浏览器检查**：原生 PNG/JPEG/WebP 导入、四风格、原生拖动/方向键、裁剪/缩放/擦除/撤销、下载备份及确认导入、游戏/工具冷启动、安静、切号、SVG 拒绝与指定档案清除。
- 特别延迟认证续期响应 3.5 秒，真实工具输入在身份恢复后保持不变；独立工具/游戏发出真实心跳，遮罩发暂停，仅携带模式/序号/标签标识。工作台真实登录显示 500 余额、当日开户入账、收藏持久化。
- **17 张唯一截图**跨机器逐项核对 SHA256 与实际 PNG/CSS 视口宽度，包含真正 **320px / 390px**；无横向溢出。人工抽查桌面工作台、320px 工作台、图片编辑及手机首页。是响应式桌面 Firefox，不冒充实体手机验收。桌宠仍是可拖动/收起的浮层，不承诺永不遮住任何正文。
- 原生图片编辑/备份交互过程中，测试代理未观察到图片请求正文或桌宠上传接口；不将既有账号/心跳请求说成“全站无网络”。
- 新鲜生产加密备份异机校验、完整恢复及最终候选映射检查通过，全部 148 表摘要、结构、索引、约束、触发器、序列和完整迁移记录比对保持不变。
- 公网 **13 项**通过：HTML、入口 JS、桌宠懒加载 JS 与最终候选哈希一致，健康与页面入口正常；今日流水、成长私有档案、支持后台匿名均 401。线上未使用真实成员账号做图片或资产测试。

## 失败记录、留存与回滚

开发阶段修正了方向键被吸附、重复暂停边界可能无法恢复、测试中旧导航名称和非法合成币种等问题；最终受影响回归与完整后端回归通过。SSH 曾间歇性断连，因此暂停切流；之后原有公钥恢复连接，完成材料回传及校验再部署。没有更改密钥、关闭主机校验或扩大权限。

合成环境两普通账号、14 钱包记录、余额合计 1000，是既有开户逻辑各 500 的结果；支持记录 0、成长时长记录 2。桌宠和只读汇总不发放办公币。合成库、日志、截图和真实加密备份分别限权留存，不进 Git，也不能把合成备份恢复到生产。

合成数据库与日志完成跨机器 SHA256 校验后，按已记录的标签、容器 ID 和独立网络核验，清理本次 5 个测试容器及其网络；可由留存合成备份恢复。生产健康复核正常。备份恢复克隆也已清理，没有删除真实玩家数据或生产数据卷。

直接应用回滚基线 **`48407006c70c5964c13ffd15e2670a322664344e`**，使用保留镜像与当批完整环境，只回 API/Web，保留 schema36 和全部新数据，不执行 DOWN 或旧库覆盖，不关账号注销。旧客户端不认识分离图片键，可能暂时显示内置形象；图片仍保留，可再次升级或通过自行导出的桌宠备份恢复。回滚也会结束临时内存对战房间；本轮未执行回滚。
