# 成长档案、称号与 VIP 自动探索发布记录

## 已部署结果

- 生产站点：<https://zbrshyyzxx.top/>，北京时间 **2026-09-09 11:36** 完成切换，11:37 公网验收通过。
- 应用提交：`727576bb8312889da3eae0795995848da787dc95`，包含功能提交 `ca2953af4051d53aabd6b4e76dfa7fce174d179b` 和最终窄屏称号修复。
- GitHub：`ZbrsGOGOGO/WebFishGogogo` 的 `feat/workstation-tower-defense`，两次均普通快进推送；未合并或强推 `main`。
- 生产目录 `/opt/webfish-review` 停在上述应用提交；Compose 项目 `webfish-community` 仅更新 API/Web，二者健康。PostgreSQL、Redis、Caddy 容器身份和数据卷未改变。
- 数据库从 `1700000000030` 到 `1700000000032`，33 条完整历史登记保留。仅新增四表，无旧业务表更新/删改迁移。
- 本发布记录在应用验收后另行提交；GitHub 因说明文档多一个提交，不表示网站仍运行旧功能。

## 用户入口和实际规则

侧栏 **成长档案**（`/achievements`）查看 12 项成就与 VIP；点击“同步成就进度”确认服务器已有的达标记录，再选择公开佩戴或卸下称号。本人和他人主页、群聊、好友私聊、轨道房间讨论展示当前佩戴的后缀。完整荣誉列表沿用隐私范围；拉黑或停用占位不暴露称号。历史消息重新读取后显示当前称号，不承诺所有已打开的旧气泡即时重绘。

**九层妖塔 → 探索 → 委托探索**，VIP 可选楼层和次数后确认。服务器每步结算；每批最多 20 次探索、260 个引擎动作、15 分钟，不是无限挂机。消耗原体力，沿用每日 200 办公币上限；不代训练/休整/购买/捐赠/打世界首领/领奖。可手动停止并接管未完成战斗。关标签页不等于注销；退出原账号会话、到期、低血、战败、资源不足、跨日或超限会停止。

此次迁移给 **12 个当时正常 active 账号**各赠送固定 720 小时 VIP：北京时间 **2026-09-09 11:36:22 至 2026-10-09 11:36:22**。聚合核验为一个统一开始时间、一个统一截止时间、错误时长 0，12 条均已生效。新注册账号不自动获赠，登录/刷新/重复迁移不续期。无收费或支付入口，不改变管理员、开发协作角色，也不额外赠送金币或战力。

## 验证证据

### 全仓与浏览器

- 后端 126 套、989 项回归通过；最终前端 127 套、888 项通过，共 1,877 项。共享/后端/前端类型检查通过；社区模式构建和正式镜像构建通过。
- 实际 Firefox 桌面和 390 CSS 像素：称号确认/取消/佩戴/卸下、好友申请接受、荣誉隐私、群聊/私聊双向实时消息、Rail 房间讨论、切号隔离、VIP/非 VIP、自动探索自然结算/离页/关原标签页/恢复/停止/注销、Esc 隐蔽页面和农场图鉴通过。
- 浏览器抓到并修复本人资料响应漏称号、拉黑后旧资料残留及窄屏长昵称裁切。最后以正常保存的 20 字符上限昵称逐行测量卡片内部边界，称号和 UUID 均未超出；并逐张检查截图，不只比较整页滚动宽度。
- VIP、25 批农场记录与两项初始成就是三个普通测试账号的显式夹具；不是自然取得的历史。妖塔角色和奖励未预造：首次自然两次探索以 15 动作完成、获 6 币；另一次确实关闭原标签页后仍继续，停止后计数保持不变。
- 浏览器 API 是隔离 HTTP 环境，启用真实定时循环，不冒充物理手机或生产 VIP 账号实玩验证。时间边界/奖励故障另由真实 PG 场景验证；不宣称大规模同时在线或长期经济压测。

### 正式候选镜像真实数据库门禁

均使用最终候选 API 镜像，而非只挂载开发编译目录。隔离库不含生产用户/备份；每套均检查精确清理及 schema32。

| 验收套件 | 场景/检查数 | 主要范围 |
| --- | ---: | --- |
| 账号与开发协作 | 9 | 真实 HTTP、身份、注销、附件/协作边界 |
| 资产 | 10 | 钱包、奖励、农场、公会原子性 |
| 社交 | 18 | 真实 PG + Redis 双实例 HTTP/WebSocket、撤销会话、好友、私聊 |
| Play | 12 | 六款游戏、单机/房间、排行、重复结算 |
| 新闻 | 11 | 真实 PG 快照；上游 fetch 明确为离线替身 |
| Rail | 11 | 房间、讨论、密码、成绩和奖励 |
| 妖塔 | 31 | 既有引擎、角色、世界、结算、注销清理 |
| 成长系统 | 8 | 固定赠送、CAS、越权、指标、HTTP/双端 WS、隐私与注销 |
| 自动探索 | 34 | 多 worker、幂等、停止竞争、会话注销、奖励回滚和最终提交前检查 |

成长套件的消息总线/审核器是明确测试替身；真实 Redis 覆盖由社交套件独立提供。注入未来时间或故障的 PG 场景不等于等待真实 30 天。最初成长演练修正了合成业务日期、绕过 Gateway 不产生广播两个测试脚本问题；失败证据保留，重跑通过。

### 备份与迁移

- 正式备份目录：服务器 `/opt/webfish-backups/growth-release-final-20260909.gPtbdt`，本机 `/home/zbrs/.local/state/webfish-backups/growth-release-final-20260909.gPtbdt`。
- 新鲜加密 PostgreSQL 自定义格式备份完整恢复到 network-none 临时 PG，迁移历史匹配；八文件清单与异机副本校验通过。目录 700、敏感文件 600，备份、密钥、环境和私人附件未进入 Git。
- 清单 SHA-256：`5a89916ee63380da189856bc199bcd95b63adb27d0940ddce18ce52e3b01b22e`。
- 另一完整备份副本执行 `0030 → 0032 → 0030 → 0032 → 无待执行项`，全部 132 张旧表内容指纹、结构、索引、已有触发器、授权和序列保持；仅迁移自身序列自然前进四次。四张新表中三表为空，会员表精确匹配迁移时 active 账号及同一事务时间/720 小时。
- 回退只在无外网、无 worker 的临时副本执行，生产没有 down 或旧备份覆盖。私人行、附件、内部用户 ID 未传到克隆比较程序，只读聚合摘要。
- 早期 `ca2953a` 的备份验证后因窄屏问题被保留但未发布，未改写其清单冒充最终版本；最终 `727576b` 使用上面的新目录、新备份和目标绑定。

### 公网与清理

- 公网只读 31 次请求，比较 11 个候选静态文件及页面外壳 SHA-256，检查缓存、安全响应头、成长/妖塔目录、三个私有端点匿名 401、旧乐斗 API 不可用、遮司同源嵌入。
- 公网普通账号 62 次请求：两名新建普通临时账号的正常登录/注销、称号本人投影、空进度同步、未拥有称号拒绝、非 VIP 自动探索拒绝，以及六次零奖励建角/属性操作的 CAS、重放和保存验证。
- 两账号、四会话、两角色、六回执、两每日行及两精确用户名限流桶已清理。钱包、奖励、世界贡献及新四表引用均为零；不删除世界，不碰共享 IP 限流或真实成员。
- 九个精确 ID/标签校验的临时测试容器及其空内部网络已移除；丢弃的仅合成 tmpfs 数据。截图、初版缺陷与复验矩阵、脚本、日志和生产备份保留。未使用 Docker 全局清理或删除任何生产卷。

## 开发反馈处理

读取七条待处理意见；“作物种植图标”已实现六种专属本地 SVG，覆盖场景、地块、图鉴，当前作物不再被下轮选择误标。上线后第一次归档被版本 CAS 拦截；重新读取发现新增一条跟进评论、原需求未改变，随后按最新版本归档成功（`submitted v2 → done v3`），留下 system 运维审计及提交者通知，未冒充管理员或扩大权限。

其余六条未整体关闭：微博官方榜单仍遇访客验证，不把外链写成站内同步成功；整套题库替换涉及删改且部分内容不适合直接上线；长篇付费/离线/职业规划、难度和视觉建议仍需后续分项。原商店售罄位置已有对应逻辑，不据此把整条综合难度反馈冒称完成。私人原文、作者资料及附件不进入公开发布文档。

## 回滚边界

回滚应用目标 `82b84312d01cd9174441dc4a5ac45d9e56ea5a71` 的镜像已保留。只能回滚 API/Web 和原环境，保留 schema32、VIP/成就/称号/自动记录及玩家资产，不执行生产 down、清空新表或恢复旧备份覆盖后续记录。

旧 API 不认识四张新个人表；若紧急启动旧 API，必须先临时关闭 `FEATURE_ACCOUNT_DELETION_ENABLED`，暂停旧注销入口和处理队列，保留原申请与日期，并尽快用前向修复恢复完整删除流程。正常发布未修改该开关。详见 [部署说明](../deploy/COMMUNITY_DEPLOYMENT.md) 与 [实现边界](COMMUNITY_PROGRESSION_20260909.md)。
