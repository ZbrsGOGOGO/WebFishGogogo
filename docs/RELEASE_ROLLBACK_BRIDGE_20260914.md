# 文字战线 v2/v3 的安全回滚桥

本分支从旧应用 `9013da770a51044965486efec9b201ae69d5f7e8` 分出，作为 v2 已上线版本及 v3 候选版本的 API/Web 兼容回滚材料，不是新功能主线。它不引入新版游戏键、客户端玩法或数据库迁移。

- 旧 Arcade 对未知游戏键的结算现明确拒绝，不能在保留的 v2/v3 对局上误走“遮司”计分逻辑；旧 `tetris`、`tank`、`zhesi`、文字战线 v1 规则保持不变。
- 保留候选可能写入玩家存档的两项免费被动技能 `s17`、`s18` 和两项免费主动技能 `s19`、`s20` 的目录、战斗效果和成长说明，确保回切后读取、配装、施放与继续结算不丢物品、不报未知技能。旧存档不自动获赠这些技能。
- 回切时保留当时 schema、全部旧/新成绩与玩家资产；不执行迁移 DOWN、不回灌旧数据库、不删运行卷。桥不结算新版 v2/v3 在途局；紧急回切前应先停止新版开局并尽量等待在途局结束，确需即时回切时需告知这些局暂不能上榜。旧 v1 在途局仍可正常结算。

上线前必须在隔离恢复的生产数据库副本上复核上述断言及 API ready；只有重新构建并验证过的桥镜像及其完整环境副本可作为本批直接回滚材料。旧桥 `1f50c07` 不认识 `s19`/`s20`，裸 `9013da7` 也不具备新版成绩防伪，均**不得**直接用于此次 v3 回滚。
