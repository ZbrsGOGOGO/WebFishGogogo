# 摸鱼指数与期权持有者：2026-09-10 发布

## 上线结果

应用 **0b991be2c3ed3aaa537e3096d8131d7045c61c09** 已推送 GitHub `main`，北京时间 **2026-09-10 11:48:52** 上线。仅切换生产 API / Web；PostgreSQL、Redis、网关容器 ID 保持不变，完整环境仅 `IMAGE_TAG` 变化。后续 README/发布记录提交不重建应用。

| 对象 | 已核验值 |
| --- | --- |
| 切换前应用 / 回滚基线 | `f46a765271a55d5708c6f6e0299c149b4183ef11` |
| API 镜像 | `sha256:284d75267ec4531b4ecf682fdb78c5fe2890274679a3b41d8da9a758bd5404e9` |
| Web 镜像 | `sha256:33ce6489ac591138a9b887894e85d139fca71a80b6d5ad2ff35138d3d924aa18` |
| 数据库 | schema35 → schema36；37 条迁移 / 148 张 public 表 |
| 唯一新增迁移 | `AddFishGrowthAndSupport1700000000036` |
| 生产支持记录 | 上线后复核为 0；未给真实用户发放支持权益或编造历史金额 |
| 所有者权限 | 只读确认 `zbrs` 仍为已有 admin；本次没有修改真实成员角色 |

## 已实现

- 有效停留及游戏页面活跃构成摸鱼指数，每日最高 360 点；单账号租约、服务器时间、重复心跳保护，不补算离线或未知历史。
- 六级称号：初入鱼场、摸鱼小将、摸鱼达人、打窝仙人、潮汐海灵、摸鱼之王。成长档案查看进度，主动佩戴后复用个人主页、聊天室与私聊后缀投影。
- VIP 界面更名「期权持有者」；原赠送期限不动，兼容协议字段与错误码保留。不代表真实股权或投资权益。
- 爱发电月度支持人工核验台账：人民币金额、月数、笔数、期限、有效和原始累计总量、分页记录、幂等授予及可审计作废。每个登记月固定 30 天，无自动支付、扣款或自动回调。
- 账号注销移除个人时长、支持台账用户/尾号与操作人关联；保留去身份化统计及防重复订单摘要。

详细规则与限制见[功能说明](COMMUNITY_FISH_SUPPORT.md)。指数不兑办公币，不增加战力；游戏页面活跃不冒充完成对局或反机器人认证。支持不授予管理权限。

## 本轮实际验证

- 全量自动回归：后端 144 个测试套件、1,366 项通过；原有 3 套件 / 23 项跳过不计成功。前端 165 个套件、1,196 项通过。
- 随后增加的注销台账断言复跑 9 项、发布检查复跑 9 项、侧栏修改复跑 20 项均通过；全仓类型检查、社区构建通过。不是声明每次小改又完整跑了一遍全量套件。
- 最终不可变镜像真实 PostgreSQL 6 组校验：重复心跳并发、10 标签页竞争、同订单重试、多笔期限串行接续、跨账号重复订单竞争、作废与称号/资产/权限边界。
- 同一最终镜像、全新合成数据库与账号：Firefox 真实操作及 HTTP 12 项通过，10 张唯一截图及 SHA256 逐张核验通过；包括真实墙钟增长、公开佩戴确认、个人页、普通用户权限、支持登记/生效/作废、390px 无横溢出和退出登录。此处不使用伪造浏览器时钟或替换业务请求。
- 最终目标的新鲜加密备份完整恢复；迁移前后的全部旧表数据摘要、结构、序列与完整迁移历史比对通过。原 146 表及赠送活动无回写，新增两表为空；异机备份逐项校验通过。
- 公网 HTTPS 13 项检查通过：健康、成长/个人/妖塔页面资源、新六级目录、真实最新 JS 文件、匿名个人档案/后台/心跳的 401 门禁。公网没有创建测试付费记录、使用真实成员账号操作或重新跑所有游戏对战。

普通浏览器会触发现有 500 办公币开户逻辑，隔离合成账号的这类默认记录单独保留审计；不将它写成浏览器阶段零资产。新功能的支持登记和时长服务自身不发办公币。

## 被拦截的准备与诊断问题

最初预检仍要求 0026 / public 项目回滚文案，与已整理的社区文档不符，停止后修正为当前最高迁移评审、独立项目和仅 API/Web 切换；没有绕过密钥、环境、备份等门禁。实看首版侧栏发现导航网格样式挤压摘要，修复后重新构建并完整复验最终镜像。

隔离诊断首次使用不同于应用的 UID，无法读取镜像内限权依赖；随后按实际应用 UID 运行。测试工作区脚本也经单文件读取权限修正。浏览器 harness 曾误用旧邮箱登录接口，带观察用 fetch 包装的尝试也未能完成计时验收；改用真实账号登录与无包装 UI 观测后重新通过。失败尝试没有作为发布凭据，未影响生产数据。

## 回滚边界

保留 f46 镜像、完整原环境与加密备份；仅回退 API/Web，**保留 schema36、新台账与上线后数据，禁止 DOWN 或恢复旧库覆盖新数据**。旧版不认识新增表，回退时暂关 `FEATURE_ACCOUNT_DELETION_ENABLED`，避免旧注销任务遗漏记录；旧版也不能识别新支持期限，须说明托管权益的临时影响并优先向前修复。回退会结束临时 Paper 房间，必须重新验收。此次未执行应用回滚。

备份、私有配置、合成凭据、截图原件及操作日志均限权留存，不进入 Git。

上线后已将最终隔离候选的合成数据库与测试日志复制到另一台机器并逐项校验 SHA256，再按已核验的容器 ID 清理本轮测试容器及独立网络。复核本轮测试标签下无遗留容器或网络，生产五个服务保持运行；合成数据可从留存备份恢复，生产数据未删除。
