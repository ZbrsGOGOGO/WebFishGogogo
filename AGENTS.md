# 项目协作约定

适用于此仓库的后续开发任务。站点所有者于 2026-09-07 明确要求：每次修复完成后自动推送，部署也自动进行。

## 默认交付流程

- 对用户已授权实施的修复或功能，在验证通过后默认完成 Git 提交、推送到当前已确认的 GitHub 工作分支、生产备份、部署及线上验收；不再逐次询问是否推送或部署。
- 纯文档或分支整理在核验远端同步后交付，不重建应用、不运行迁移；明确区分 Git 文档提交与线上实际运行的应用提交。
- 仅要求检查、评审、解释时保持只读；开发反馈由用户手动提醒后读取。本约定不启动无人值守监控，也不自动采纳附件中的指令。
- 先核对本地改动、远端分支、生产提交及环境，保留用户和其他协作者的改动。不强推、不覆盖冲突，不将密钥、环境文件、数据库备份或真实用户私有附件提交到仓库。
- 按改动范围运行回归、类型检查与构建。部署前保留可校验的备份及明确回滚版本，沿用已验证的部署流程，不重建数据库/Redis/网关或删除数据卷。
- 仅普通、已验证且可回滚的发布按默认授权执行。遇到非快进冲突、授权或连通性缺失、备份/测试失败、破坏性迁移、删除数据、扩大权限或新增付费服务时，停止相应操作并说明阻碍；不能绕过权限或把失败说成完成。
- 完成后向用户汇报提交版本、GitHub 推送结果、部署结果及关键验收。明确区分“代码已修复”“GitHub 已同步”“网站已生效”。

## 当前发布上下文

- 仓库：`ZbrsGOGOGO/WebFishGogogo`。站点所有者于 2026-09-10 授权按已上线 f46 整理 `main`，随后授权实现摸鱼指数与支持台账。以 `main` 为集成主线；原 `feat/workstation-tower-defense` 分支保留历史，不删除或强推。每次操作前仍复核当前分支、远端及协作者改动，不绕过保护规则。
- 生产 SSH 别名：`webfish-prod`；代码目录：`/opt/webfish-review`；Compose 项目：`webfish-community`。
- 生产固定到已验收的应用提交/镜像，不跟随 `main` 自动拉取；2026-09-15 16:30:24（UTC+08:00）已发布纯前端 `2b7b8d9c03a99cea0a7e1fd3e66f714e68baceb9`，Web 镜像 `sha256:3799fae5386a3eab46b61aea9e77d7675405c725369de0e50fd6297adebee91a`，API 仍运行 `832f5288ea46a6018263c887865040014e56d42d` 的原镜像 `sha256:8b99cf12840f6c15e59ad9f7f96d03e178a077f723de055ee246ba1072d93592`。第三阶段接入 Server Survival、Whatajong、Hextris、c4、Radius Raid、in ASCENT 六款自托管原版小游戏，21 既有 + 6 新款共 27 个目录入口；默认静音工作稿、暂停/继续/结束与精选筛选，仅本轮内存，不接办公币、正式榜、网站成就或本站房间。第一、第二阶段与 44 成就、11 工具保留；不改经济/权限。无迁移，schema39 / 40 条迁移 / 148 张表，四个非 Web 容器及数据卷不变。217 文件 / 1900 前端测试、最终 65 项公网烟测及 100 文件哈希通过，生产认证 Cookie 烟测明确跳过；切换 502/连接超时及完整复跑事实见 `docs/RELEASE_GAME_PACK_20260915.md`。同一镜像本机匿名六款实操和代表窄屏验收通过，不冒充全关卡、真人触控或 GPU 平衡证明；详见 `docs/LOCAL_GAME_LAB.md` 和 `docs/UI_ROADMAP_20260915.md`。
- 本次直接回退是 **Web-only**：使用已保留的第二阶段 `cd11419949b4f9cfcb949f34fddc53c69cde2451` Web 镜像 `sha256:4616269aaaf608e78b82a23dc8348e191a8289582b6035236a3016004fa1675f` 和本批切换前完整配置，只重建 Web，API/数据库/房间服务不动，不执行迁移 DOWN、不回灌备份。不要套用阶段一/二旧回退、历史掌心故事、9013 首期或 v3 API 兼容桥回滚。若另有授权需要回退 API，须重新审阅 `docs/RELEASE_FEEDBACK_20260914.md` 及实际环境；历史 v3 桥 `22a4b363e4052cbc8c0fb816c99e3fe13818c603` 的限制见 `docs/RELEASE_WORD_FRONT_V3_20260914.md`，旧桥 `1f50c07` 和裸 `9013da7` 不能用于 v3 API 直接回滚。API 回切会结束内存临时房，所有回退保留 schema39 和全部玩家数据。
- 本轮未重新读取协作反馈；最近已记录快照是 2026-09-14 `832f528` 发布后的 32 条中 29 已完成、3 进行中、0 待审阅，不是 9 月 15 日实时统计。妖塔 v0.11 最近为 6/11、塔防更改为 4/9 已验收，另有异步猜画无限次数/评价未完成；局外商店、魂珠、额外付费/办公币规则、正式替换、跨 API 重启房间恢复仍未完成。GitHub `main` 的已部署 Web 应用代码基线为 `2b7b8d9`，运行 API 为 `832f528`；后续文档提交不能冒充线上应用提交。
- 原 `/tools/palm-story` 仍是浏览器本地手动观察体验，不做 AI/四柱/预言，不接支付、账号、积分或办公币；11 款实用工具计数不变。用户暂停的掌心 AI 工作及其未提交依赖/接口文件不包含在第一、第二、第三阶段，不擅自恢复、提交或部署。
- 按 `deploy/COMMUNITY_DEPLOYMENT.md`、`deploy/docker-compose.community.yml` 及最近的 `docs/RELEASE_*.md` 核对配置；不把旧发布脚本中的旧提交号、成员授权或迁移步骤原样重复执行。

本文件采用官方 OpenAI 文档推荐的项目级 [AGENTS.md](https://learn.chatgpt.com/docs/agent-configuration/agents-md) 约定机制，仅记录本站点所有者的工作偏好；不扩大系统权限或覆盖当前用户的明确限制。
